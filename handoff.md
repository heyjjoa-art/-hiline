# 작업 현황 (Handoff)

마지막 업데이트: 2026-08-10

## 완료 (2026-08-12 — PDF 합치기 + 두 PDF 메뉴 통합 + 필요서류 메뉴 + 메뉴 순서 변경)
사용자가 연달아 요청한 4건을 한 세션에서 처리함.

### 1) PDF 합치기 (`tools/pdf-merge.js` 신규)
- 여러 PDF/이미지를 한 PDF로. 목록 순서대로 이어붙이고 **행마다 ▲▼로 순서 변경**(사용자가
  "합칠때 순서도 설정할 수 있어야함"이라고 확인 요청한 부분), 파일명 순 정렬(`Intl.Collator`의
  `numeric:true`라 1,2,10 순으로 제대로 감), 개별 삭제, 목록 비우기, 드롭할 때마다 뒤에 이어붙임.
- 이미지 페이지 크기 선택: **A4에 맞춤(비율 유지·가운데, 기본)** / 이미지 크기 그대로.
- pdf-lib는 JPEG/PNG만 직접 임베드 가능 → 그 외(webp/gif/bmp)는 캔버스로 PNG 변환 후 삽입.
- 실패 시 어느 파일에서 막혔는지 파일명을 붙여 알려준다.
- **검증**: 쪽마다 크기가 다른 PDF 2개 + 실제 PNG/JPEG로 — 합친 쪽수·쪽 크기 순서가 정확히
  일치(A→B, B→A 순서 반영), PDF+이미지 혼합 7쪽, A4 맞춤 시 이미지 쪽이 595x842, "크기 그대로"
  시 원본 200x100, 진행 콜백, 빈 목록·깨진 파일 오류 메시지까지 전부 통과.

### 2) PDF 메뉴 2개를 하나로 통합 (사용자 요청)
- "PDF 파일 편집기"와 "PDF 파일 변환"을 **"PDF 편집기" 메뉴 하나**로 합치고 안에서 탭 전환:
  **나누기(분할·쪽 추출)** / **합치기(여러 PDF·이미지 → 1개)**.
- 두 도구는 파일이 따로지만 엘리먼트 id가 안 겹쳐서(`#pdf-*` vs `#pm-*`) 같은 root에 나란히
  초기화한다. 탭 전환 로직은 `sectionInit["pdf-editor"]` 안에 둠.
- `pdf-merge` 메뉴 항목·섹션 렌더 함수는 제거.

### 3) 필요서류 메뉴 (`tools/required-docs.js` 신규)
- 통장사본·사업자등록증처럼 자주 인쇄/제출하는 파일을 등록해두고 바로 인쇄·다운로드.
  항목별 인쇄/다운로드/이름변경/삭제 + 체크박스로 여러 건 골라 **한 PDF로 합쳐서** 인쇄·저장
  (통장사본+사업자등록증을 같이 제출하는 실제 용도).
- **보관 위치가 이 기능의 핵심 제약**: 서버가 없으므로 브라우저 안에 저장한다.
  IndexedDB(원본 Blob 그대로)를 우선 쓰고, **열기 자체가 막히는 환경이면 자동으로 localStorage
  (base64)로 떨어진다**. `file://`에서 IndexedDB 정책이 브라우저마다 달라서 실제로 열어보고
  판단하는 방식(`pickBackend`). 지금 어느 쪽을 쓰는지 화면 하단에 표시하고, localStorage로
  떨어졌으면 "용량이 좁으니 큰 파일은 피해달라"고 같이 안내한다. 용량 초과 시 사람이 읽을 수
  있는 메시지로 바꿔서 던진다.
- **인쇄는 항상 PDF로 변환해서 한다** — 이미지를 그대로 인쇄하면 배율이 제각각이라
  `pdf-merge`의 `mergeToPdf`를 재사용해 A4 맞춤 PDF로 만든 뒤 인쇄. 그래서 `required-docs.js`는
  `pdf-merge.js`보다 뒤에 로드해야 함(index.html 순서 확인함).
- 인쇄창은 새 창 대신 **숨긴 iframe**으로 띄운다(팝업 차단 회피). 실패하면 "다운로드로 받아서
  인쇄해달라"고 안내.
- 화면에 "이 브라우저에만 저장됨 / 브라우저 데이터를 지우면 같이 지워짐 / 원본은 따로 보관 권장"
  주의 카드를 같이 뒀다.
- **검증**: IndexedDB가 없는 환경(= localStorage 대체 경로)을 재현해서 실제 함수 호출로 —
  보관소 자동 선택, 종류 판별, 등록→목록(blob 복원)→이름변경→삭제, 재조회 시 유지,
  선택 3건 합쳐 4쪽 PDF 생성(이미지 쪽 A4), 단건 1쪽, 용량 초과 메시지까지 전부 통과.

### 4) 인덱스 메뉴 순서 변경
- 사이드바 메뉴를 **끌어다 놓아 순서 변경**(HTML5 드래그앤드롭). 칸의 위/아래 절반 어디에
  놓느냐로 앞/뒤를 정하고, 놓일 자리는 `box-shadow: inset`으로 선을 그린다(border로 그리면
  항목 높이가 흔들려서). 순서는 `localStorage["hiline_menu_order"]`에 id 배열로 저장.
- `applySavedOrder()` — **나중에 메뉴가 추가돼도 저장된 순서에 없다는 이유로 사라지지 않고 뒤에
  붙는다**. 저장된 순서에만 있고 지금은 없는 id는 무시. 이 프로젝트는 메뉴를 계속 추가하는
  구조라 이 처리가 없으면 새 메뉴가 안 보이게 될 수 있었음.
- 사이드바 아래에 "순서 초기화" 버튼(사이드바 접으면 같이 숨김).
- **검증**: `applySavedOrder`를 떼어내 6케이스(저장값 없음/빈 배열/완전 역순/일부만 저장/
  없어진 id 포함/그대로) 전부 통과.

- **공통 검증**: `node --check` 3개 파일, `index.html` 인라인 script 파싱, 메뉴·섹션·init·script
  로드 순서·JS가 찾는 엘리먼트 id 누락 여부·섹션 div 균형 확인.
- **브라우저 미연결로 실제 화면 확인은 못함** — 특히 **(a) `file://`에서 IndexedDB가 열리는지
  (안 열리면 localStorage로 떨어지고 화면에 그렇게 표시됨), (b) iframe 인쇄창이 실제로 뜨는지,
  (c) 메뉴 드래그가 클릭 이동과 충돌하지 않는지** 는 사용자 확인 필요.

## 완료 (2026-08-11 — PDF 파일 편집기 메뉴 신설: 낱장 분할 / 원하는 쪽만 골라 한 파일로)
- 사용자 요청: "인덱스 메뉴추가 / PDF파일 편집기 / PDF 파일을 올리면 1장씩 분할저장 하거나
  원하는 페이지만 선택해서 1개파일로 만드는 기능".
- **라이브러리 신규 추가**: `vendor/pdf-lib.min.js`(1.17.1, 513KB), `vendor/jszip.min.js`(3.10.1, 95KB).
  unpkg에서 받아 로컬에 저장(이 프로젝트는 `file://`로 여는 전제라 CDN 못 씀). CLAUDE.md 기술 스택에도 기재.
  - **낱장 분할은 파일이 여러 개 나오는데 브라우저가 연속 다운로드를 막는 경우가 있어 ZIP 하나로
    묶어서 내려준다**(그래서 JSZip이 필요).
  - **썸네일 미리보기는 넣지 않음** — pdf.js가 필요한데 `file://`에서 워커 로딩이 막혀 쓰기 어렵다.
    대신 쪽 번호 체크박스 그리드 + 범위 입력칸("1-3, 5, 8-")으로 고르게 했고 둘이 서로 실시간 반영됨.
- **신규 파일 `tools/pdf-editor.js`**:
  - `parsePageRange()` — "1-3, 5, 8-"(끝 비우면 마지막 쪽까지) 파싱. 잘못된 부분이 있어도 통째로
    실패시키지 않고 `errors`에 모아 화면에 보여준다(범위 초과/역순/형식 오류 각각 다른 메시지).
  - `formatPageRange()` — 선택 목록을 "1-3, 5, 8-10"으로 되돌려 표시.
  - `splitToZip()` — 쪽마다 1쪽짜리 PDF를 만들어 ZIP으로. 파일명 `원본이름_01.pdf`이고
    **총 쪽수 자릿수에 맞춰 0을 채운다**(100쪽이면 `_001`) — 파일 정렬이 어긋나지 않게.
    진행 중 `onProgress`로 "n/전체쪽" 표시(페이지 많으면 오래 걸려서), 매 쪽마다 이벤트 루프를
    한 번 넘겨 화면이 멈춘 것처럼 보이지 않게 함.
  - `mergeToOne()` — 고른 쪽만 담은 PDF 1개. 암호 걸린 PDF도 열리게 `ignoreEncryption: true`.
  - 처리 중에는 버튼을 잠그고(`setBusy`), 실패 시 상태 메시지에 원인을 그대로 노출.
- **`index.html`**: 메뉴 항목 `pdf-editor` 추가(맨 아래) / 섹션(드롭존 + "1) 낱장으로 분할" +
  "2) 원하는 쪽만 골라 한 파일로" 2단 구성) / `sectionInit` 등록 / vendor·tools script 태그 /
  `.pdf-*` CSS 신규(쪽 칩 그리드는 `max-height:220px` + 세로 스크롤이라 쪽이 많아도 화면을 안 밀어냄).
  드롭존 라벨은 세금계산서 비교와 같은 `<span class="pdf-zone-label">` 패턴(innerHTML 통째 교체로
  내부 `<input type=file>`이 날아가는 문제 회피).
- **검증**(Node에서 실제 모듈 함수 호출):
  - 범위 파싱 12케이스 — 정상/공백 포함/끝 열린 범위/역순/0/초과/문자/`1--3`/빈 문자열 전부 의도대로.
  - **쪽 동일성 확인**: 쪽마다 크기가 다른 PDF를 만들어(1쪽=110x210 … 10쪽=200x300) 병합·분할
    결과의 페이지 크기를 대조 — 선택 [1,3,5,10] / [10,1](주어진 순서 유지) / [7] / 전체, 그리고
    낱장 10개가 각각 원본 1~10쪽과 일치하는 것까지 전부 통과. "쪽수만 맞고 엉뚱한 쪽이 들어가는"
    경우를 실제로 배제함.
  - 12쪽 테스트 PDF로 ZIP 12개 생성·각 파일이 1쪽인지 확인, 100쪽으로 자릿수 패딩(`_001`~`_100`) 확인.
  - **DOM 스텁으로 `init()` 전체 흐름 실행**: 파일 로드 → 안내문/칩 12개/기본 전체선택("1-12") →
    범위 "2-4, 9" 입력 시 칩 체크가 2,3,4,9로 동기화 → 잘못된 범위 "99" 경고 → 전체 해제 후 병합
    시도 시 "쪽을 하나 이상 골라주세요" → 병합 저장 → 낱장 분할 → 버튼 잠금 해제까지 전부 통과.
    다운로드 파일명 `월간보고서_선택3쪽.pdf`, `월간보고서_낱장.zip` 확인.
  - `node --check` 통과, `index.html` 인라인 script 파싱 + 메뉴/섹션/init/script 태그/vendor 로드
    순서(vendor가 tools보다 먼저) 확인.
  - **사용자가 실제 화면에서 확인 완료**(2026-08-11). (이 세션은 브라우저 미연결이라 나는 직접
    못 봤고, 사용자 확인으로 갈음.)

## 완료 (2026-08-11 — 비교 후 업로드 파일 자동 삭제 + 로컬 샘플 데이터 정리)
- 사용자 요청 "비교후 파일 삭제하는것으로 진행해줘" — 화면/로컬 둘 다로 확정(AskUserQuestion).
- **화면**: 비교가 끝나면 `clearUploadedFiles()`로 올린 파일을 즉시 놓아준다(드롭존 라벨 원복,
  `input.value=""`, `files.hw/nt=null`). 상태 메시지에 "올린 파일은 화면에서 삭제했습니다" 추가.
  결과 표와 비교자료 다운로드는 그대로 동작 — 결과는 이미 값으로 뽑아둔 상태라 원본이 필요 없다.
  - 이를 위해 드롭존 마크업에 `<span class="tsc-zone-label">`을 도입하고 라벨만 갈아끼우도록 변경.
    **기존엔 `zone.innerHTML`을 통째로 갈아서 그 안의 `<input type="file">`이 DOM에서 날아가고
    있었음**(참조를 붙들고 있어 동작은 했지만 초기화 시 새 input이 생겨 리스너와 어긋날 구조였음).
  - 오류로 실패한 경우엔 일부러 안 지운다 — 다른 파일로 다시 시도하기 쉽게.
- **검증**: 최소 DOM 스텁으로 `init()`을 Node에서 실제 실행 — 파일 선택 → 비교 → (a) 두 드롭존
  라벨이 기본 안내로 복원되고 `input.value`가 빈 문자열이 되는지, (b) 결과 표(362건)가 남아있는지,
  (c) **파일을 지운 뒤에도 비교자료 다운로드가 성공하는지**, (d) 파일 없이 비교를 다시 누르면
  "각각 하나씩 올려주세요"가 뜨는지 — 전부 통과.
- **로컬 샘플 데이터 삭제**: `etc/국세청파일7월.xls`, `etc/하이웍스파일7월.xlsx`(웹페이지 껍데기),
  `etc/하이웍스파일7월.files/`(2.2MB) 삭제. 이미 `.gitignore`에 있어 git에는 영향 없음.
  **다음에 이 기능을 다시 검증하려면 사용자가 월별 파일을 받아 `etc/`에 다시 넣어야 함.**
  `etc/`에 남은 파일: 2026.07.15 세금계산서 발행건.xlsx / 대량등록 양식 2종 / 지로납부번호.xlsx.

## 완료 (2026-08-10 — 세금계산서 합계표 비교 기능 구현: 하이웍스 ↔ 국세청 월별 대조)
- **사용자 확정 사항**: 비교 대상은 하이웍스 ↔ 국세청(회계사랑은 나중에 별도) / 하이웍스는 **월별**
  비교(회계사랑은 나중에 분기별) / **청구·영수 구분은 비교 안 함** / **수정세금계산서는 원건과
  상계하지 말고 각각 별도 1건** / 주민번호 발행건 번호 불일치는 **하이웍스 엑셀 추출 버그**
  (651029의 9가 한 번 더 들어가고 끝자리 3이 잘림, 이재원도 동일)라 **무시**.
- **신규 파일 `tools/tax-summary-compare.js`**:
  - 읽기는 **SheetJS**(두 파일 다 BIFF8 `.xls`라 ExcelJS로는 못 읽음), 결과 엑셀 쓰기는 ExcelJS.
    `cellNF`/`cellText` 옵션은 이 파일들에서 셀 값이 깨지게 읽히는 경우가 있어 쓰지 않음(주석에 명시).
  - `parseHiworks`: 헤더 행을 "과세유형"+"공급받는자"로 찾아서(고정 행 번호 가정 안 함) 그 다음 행부터
    파싱. 열은 0-indexed 1/2/3 년월일, 4 등록번호, 6 상호, 19 공급가액, 20 세액.
  - `parseHometax`: 헤더 행을 "승인번호"+"공급받는자사업자등록번호"로 찾음. **"상호"가 공급자·
    공급받는자 두 번 나오므로 공급받는자 등록번호 뒤쪽 것을 골라 씀**(이거 안 하면 전부 하이라인닷넷).
  - `compare`: 4단계 그리디 매칭 — (1) 사업자번호+작성일자+공급가액+세액 (2) 상호+작성일자+금액
    (주민번호 자릿수 밀림 구제, 조용히 일치 처리) (3) 사업자번호+작성일자 → "금액 차이"
    (4) 상호+작성일자 → "금액 차이". 남은 건은 "하이웍스 누락"/"국세청 누락".
    `usedHw` Set으로 같은 청구건이 두 행에 중복 소진되지 않게 함(같은 거래처·같은 날·같은 금액
    2건이면 각각 따로 짝지어짐).
  - 귀속월이 여러 개 섞였거나 두 파일 월이 다르면 결과 위에 경고 배너 표시(월별 비교 전제).
  - 결과 엑셀 다운로드(ExcelJS, 헤더 배경/테두리/굵게 + 금액칸 `#,##0` 우정렬).
    **사용자 추가 요청("차이없더라도 비교자료 다운받을 수 있도록") 반영** — 다운로드 버튼을
    차이 0건일 때도 항상 노출하고, 엑셀을 시트 3개로 구성:
    (1) **요약** — 건수/공급가액/세액을 국세청·하이웍스·차이로 나란히(차이 나면 빨간 굵게) +
        귀속월·짝이 맞은 건수·차이 건수·비교 일시,
    (2) **차이목록** — 차이 건만. 차이가 없으면 빈 시트로 보이지 않게 "차이 없음" 안내 행을
        병합해서 넣음,
    (3) **전체대조** — 짝이 맞은 건까지 포함한 전 건 목록(작성일자→상호 순, 차이 건은 "구분"칸을
        빨간 굵게 표시해 섞여 있어도 찾기 쉽게).
    이를 위해 `compare()`가 `rows`(일치 포함 전체 대조 내역)를 반환하도록 바꾸고 `diffs`는
    거기서 걸러 쓰도록 재구성함(`matchedCount`도 `rows` 기준으로 계산).
- **`index.html`**: `tools/tax-summary-compare.js` script 태그 추가 / 섹션을 업로드 2개 + 비교 버튼 +
  요약표 + 차이 목록 표로 교체(대조 기준 안내 카드 포함) / `sectionInit` 등록 / `wideSections` 추가 /
  `.tsc-*` 전용 CSS 신규. **결과 표에 `.wf-table`을 쓰지 않음** — `.wf-table:not(.wf-leave-table)`의
  `table-layout:fixed`와 input 전용 스타일이 읽기 전용 표에는 안 맞고, 과거 밀림 버그의 원인이었던
  조합이라 자체 클래스로 분리함.
- **검증**: 브라우저 IIFE를 Node에서 `new Function()`으로 실행해 **실제 모듈 함수를 그대로 호출**해서 확인 —
  - 국세청 파서: 실파일 362건 파싱, 합계가 파일 3행 표기 총계와 정확히 일치(공급가액 202,085,672 /
    세액 20,208,554), 공급받는자 상호에 공급자(하이라인닷넷)가 섞이지 않음(0건), 수정세금계산서
    4건 음수 그대로 파싱.
  - 하이웍스 파서: 원본 샘플이 소실돼(아래) 같은 전양식 레이아웃인 `2026.07.15 세금계산서 발행건.xlsx`로
    대체 검증 — 203건, 날짜 8자리 정규화 전부 통과.
  - 대조 규칙: 완전일치/금액차이/주민번호 폴백(누락으로 오탐 안 됨)/양쪽 누락/동일건 2vs1 중복소진
    방지/수정세금계산서 음수 1:1 매칭/귀속월 불일치 감지 — 전부 통과.
  - 결과 엑셀: 실제 `buildResultWorkbook` 호출해 행 구성·헤더 서식·numFmt·정렬 확인 후 write 성공.
  - `node --check` 통과, `index.html` 인라인 script `new Function()` 파싱 통과, script 태그/sectionInit/
    wideSections 등록 확인.
  - **실파일 통합 검증 통과**: 하이웍스 362건 / 국세청 362건, 공급가액·세액 합계 차이 0,
    귀속월 양쪽 202607, **362건 전부 짝이 맞고 차이 0건**. 주민번호 발행건 2건(최종현/이재원)도
    상호 폴백으로 정상 매칭됨(폴백이 없으면 누락 2건으로 오탐되는 게 확인됨).
  - **사용자가 실제 화면에서 확인 완료**(2026-08-10) — 업로드 2칸·비교 버튼·결과 표·비교자료
    다운로드까지 화면에서 동작하는 것을 사용자가 직접 확인함. (이 세션은 브라우저 미연결이라
    나는 직접 못 봤고, 사용자 확인으로 갈음.)
- **`etc/하이웍스파일7월.xls`가 "웹 페이지" 저장본으로 바뀐 건**: 15:08에 950,486바이트(진짜 BIFF8)였던
  것이 15:37에 **10,060바이트짜리 프레임셋 HTML**(`<html xmlns:v=...>` + `fnBuildFrameset` 스크립트)로
  덮어써지고, 실제 데이터는 옆에 새로 생긴 `etc/하이웍스파일7월.files/sheet001.htm`(2.2MB)으로 빠졌음.
  엑셀에서 "웹 페이지(*.htm)"로 저장하면 이렇게 갈라지는데, **엑셀로 열면 멀쩡해 보여서** 원인을
  찾기 어려움(사용자도 "파일은 문제 없는 것 같다"고 봤음). 데이터 자체는 `.files\sheet001.htm`에
  살아 있고 SheetJS가 HTML 표도 읽을 수 있어서 그걸로 위 실파일 검증을 마쳤음.
  - 이 함정이 실사용에서도 반복될 수 있어 `isWebPageStub()`으로 감지해서 **"웹 페이지로 저장된
    파일이라 데이터가 없다 / 원본을 올리거나 .xlsx로 다시 저장해 달라"**는 안내를 띄우도록 함.
    감지 테스트: stub=true, 정상 .xls/.xlsx/실데이터 sheet001.htm=false(오탐 없음) 확인.
  - 사용자가 화면에서 이 파일을 올려 "헤더에서 '과세유형'을 찾지 못했습니다" 오류를 실제로 만남
    (`isWebPageStub` 추가 전 버전이라 원인이 안 드러나는 메시지였음). 이걸 계기로 파서 보강:
    (1) 첫 시트만 보던 걸 `findSheet()`로 **모든 시트를 훑어** 헤더가 있는 시트를 찾도록 변경,
    (2) 헤더 판정을 단어 하나가 아니라 3가지 조합 중 하나로 완화(`isHiworksHeader`),
    (3) 스캔 행 수 10→20, (4) 실패 시 **실제로 읽힌 시트명·행수·첫 내용을 오류 메시지에 붙여**
    원인을 바로 알 수 있게 함(`describeSheets`). 파서 시그니처가 rows→workbook으로 바뀜.
    회귀 확인: 실파일 재검증 362건 차이 0 유지, 전양식 xlsx 203건 파싱 유지.

## 참고 (2026-08-10 — 소실 전 하이웍스/국세청 샘플 파일 구조 분석)
- 사용자가 `etc/`에 샘플 투입: `국세청파일7월.xls`, `하이웍스파일7월.xls`.
  **비교 대상은 "하이웍스 ↔ 국세청"으로 확정**(사용자 확인: "하이웍스가 맞아. 회계사랑은 나중에
  다시 이야기할게"). 위 항목의 "회계사랑" 표현은 하이웍스로 대체됨.
- 두 파일 다 진짜 BIFF8 `.xls`(OLE, `D0CF11E0`) — ExcelJS는 못 읽으므로 이 기능은 **SheetJS
  (`vendor/xlsx.full.min.js`)로 읽어야 함**. 읽기 전용이라 SheetJS 스타일 제약과 무관.
- **국세청파일 구조**: 시트 1개 `"세금계산서"`, `A1:AG368`. 1행=사업자번호/상호/대표자명,
  3행=총 합계금액 222,294,226 / 총 공급가액 202,085,672 / 총 세액 20,208,554,
  5행=병합 제목("매출 전자(수정) 세금계산서 목록조회" A5:Y5 + "대표 품목" Z5:AG5),
  **6행=헤더(33열)**, 7행~368행=데이터 362건. 승인번호가 전부 유니크 → 1행 = 계산서 1건.
  주요 열: 작성일자/승인번호/발급일자/전송일자/공급자사업자등록번호/…/공급받는자사업자등록번호/
  상호(공급받는자, **"상호"가 공급자·공급받는자 2번 나오므로 두 번째 것을 써야 함**)/합계금액/
  공급가액/세액/전자세금계산서분류/종류/발급유형/비고/영수·청구 구분/이메일 + 품목 8열.
  - 매출만 있고 **매입 시트는 없음** → 현재 샘플로는 매출 대조만 가능.
  - 품목은 "대표 품목" 1개만 실려 있어서 품목공급가액 합계 ≠ 공급가액인 건이 104건 —
    **품목 단위 대조는 이 파일로 불가능**, 계산서 단위로만 대조해야 함.
  - 수정세금계산서 4건(분류="수정세금계산서") — 브로던 -408,000/-288,000, 항성트레이딩
    -94,500/+52,500 처럼 **음수 금액이 들어있음**.
- **하이웍스파일 구조**: 시트 1개 `Sheet1`, **63열, 1행이 헤더, 2행~363행 데이터 362건**.
  `etc/대량등록 양식 (정발행).xlsx`와 같은 대량등록 양식 포맷. 1행 = 계산서 1건이고 품목은
  품목1~4까지 가로로 최대 4개. 작성일자가 년(B)/월(C)/일(D) 3열로 쪼개져 있고 사업자번호(E)는
  하이픈 포함 문자열. 전부 과세(A), 전부 2026년 7월.
- **실제 대조 결과(scratchpad 스크립트, 사업자번호+작성일자+공급가액+세액 4개로 1:1 매칭)**:
  - **360/362건 일치**, 총 공급가액·총 세액은 양쪽 완전 일치(202,085,672 / 20,208,554).
  - **불일치 2건 = 주민번호 발행건**(최종현 69,000 / 이재원 150,000). 사업자번호 자리에 들어간
    주민등록번호 13자리가 두 파일에서 서로 다름(예: 국세청 `651029-1069113` vs 하이웍스
    `651029-9106911` — 뒷자리 배열이 어긋남). 금액·상호·작성일자는 같음. → 매칭 키를 사업자번호로만
    잡으면 이런 건이 "누락"으로 오탐되므로, **주민번호 건은 상호+금액 폴백 매칭이 필요**.
  - 하이웍스 파일 내부 정합성 문제 6건: 품목별 세액 합계가 헤더 세액과 **1원** 다름(반올림).
    예: 레드마우스 128,182 vs 128,181. 국세청 대조에는 영향 없음(헤더 세액 기준으로 맞음).
  - **청구/영수 구분이 대량 불일치**: 국세청 청구 356 / 영수 6 vs 하이웍스 C(청구) 74 / R(영수) 288.
    → 이 항목을 대조 대상에 넣을지 사용자 확인 필요(넣으면 300건 가까이 차이로 뜸).
- **다음 단계(사용자 결정 대기)**: (1) 매칭 키/주민번호 폴백 규칙, (2) 대조 항목 범위(공급가액·
  세액·건수만 vs 청구영수·품목까지), (3) 수정세금계산서(음수) 취급, (4) 비교 단위(월/분기 —
  현재 샘플은 둘 다 7월 단일). 확정되면 `tools/tax-summary-compare.js` 작성 → 섹션을 업로드
  2개 + 결과 표로 교체 → `sectionInit` 등록 → `wideSections` 추가 검토.

## 완료 (2026-08-10 — 인덱스 메뉴에 "세금계산서 합계표 비교" 추가, 화면 틀만)
- 사용자 요청: 인덱스페이지 메뉴에 "세금계산서 합계표 비교" 추가. 범위는 AskUserQuestion으로
  확인해 **메뉴 + 빈 화면 틀만**으로 확정(비교 로직은 실제 파일 샘플을 받은 뒤 다음 단계).
- 추가 맥락(사용자): **회계사랑**과 **국세청(홈택스)** 파일을 월별·분기별로 자동 비교하는 기능이며,
  두 사이트 **로그인은 사용자가 수동으로 진행**하고 내려받은 파일만 화면에 올려 비교하는 방식.
  → 지로/HIMS처럼 도우미 서버(Playwright)로 로그인 자동화할 필요 없이, 브라우저 안 파일 업로드
  방식으로 구현하면 됨.
- `index.html` 수정 2곳: `menuItems`에 `{ id: "tax-summary-compare", label: "세금계산서 합계표 비교" }`를
  "세금계산서 양식수정" 바로 다음에 추가 / `sections`에 `"tax-summary-compare"` 렌더 함수 추가
  (설명 문단 + "준비 중" 카드에 앞으로 확정해야 할 항목 4가지 나열: 회계사랑 파일 형식, 국세청
  합계표 형식(매출/매입 구분), 비교 단위(월/분기), 대조 기준(사업자번호·공급가액·세액·건수)).
  `sectionInit`에는 등록 안 함(JS 로직 없음), `wideSections`에도 아직 미포함(표가 생기면 추가 검토).
- **다음 단계**: 사용자가 회계사랑/국세청 샘플 파일을 `etc/`에 넣어주면 그 구조에 맞춰
  `tools/tax-summary-compare.js` 작성 → 섹션 HTML을 업로드 2개 + 결과 표로 교체 → `sectionInit` 등록.
- **검증**: `<script>` 블록 전체를 `new Function()`으로 파싱해 문법 오류 없음 확인, 메뉴 배열에
  새 항목이 들어가고 섹션 렌더 함수가 정의된 것도 확인. 브라우저 미연결로 실제 화면에서 메뉴
  클릭 시 섹션이 뜨는지는 사용자 확인 필요.

## 완료 (2026-08-06 — 복지제도 관리 엑셀 다운로드: "복지포인트 내역" 탭 열 너비 + 완료/올해 색상)
- 사용자 요청: 텍스트 잘리지 않게 가로 사이즈 조정 / 완료건은 인덱스 페이지처럼 회색 표시 /
  올해건은 노랑 표시.
- `tools/welfare-management.js`의 `addPointsSheet` 열 너비를 내용 길이에 맞게 재조정
  (팀명12/성명·직급8/입사일10/근속연수8/지급포인트·잔여한도16(콤마+"원" 붙어 길어짐)/
  사용내역26("N건 (합계 W원)" 요약문)/장기근속(5주년)·장기근속(10주년)18(헤더 라벨이 김)/
  41세검진·부모님검진13). 기존엔 일괄 12~20이라 특히 사용내역·장기근속 헤더가 잘려 보였음.
- 인덱스 페이지의 `completionCell` 배경색 규칙(값이 "완료"로 끝나면 회색 `#d9d9d9`, 완료가
  아니면서 올해 연도가 포함되면 노랑 `#fef3c7`)을 엑셀에도 그대로 재현하는 `pointsMilestoneFill`
  함수를 추가해서 장기근속(5주년)/장기근속(10주년)/41세검진/부모님검진(I~L열) 데이터 칸에 적용.
  `POINTS_YEAR_FILL`(#fef3c7) 색상 상수 신규 추가, 회색은 기존 `LEAVE_GRAY_FILL` 재사용.
- **검증**: 실제 `buildWorkbook`을 Node에서 직접 호출해(전역에 `window`/`ExcelJS` 셔밍) 생성된
  워크북을 검사 — 열 너비 값, "완료"/"2026 완료" 같은 값의 회색 처리, "2026 해당"(현재
  연도=2026)의 노랑 처리, "2030 해당"(다른 연도)이나 빈 값은 무색으로 남는 것까지 전부 확인함
  (앞선 작업의 scratchpad 복제 테스트보다 한 단계 더 정확한 검증 — 이번엔 실제 모듈 함수를 그대로
  호출). 브라우저 미연결로 실제 Excel 화면에서 최종 확인은 못함 — 사용자가 다운로드해서 확인 필요.

## 완료 (2026-08-06 — 복지제도 관리 엑셀 다운로드: "복지포인트 내역" 탭 헤더 병합 + 금액 서식)
- 사용자 요청: "복지포인트" 제목(F~H열)과 "개인별복지사항" 제목(I~L열)을 1~2행 전체 병합 /
  지급포인트·잔여한도 칸에 4번째 줄(=데이터 첫 행) 이후 금액 쉼표 표시 및 우정렬.
- 기존엔 그룹 제목이 1행에만 걸쳐 있고(F1:H1, I1:L1) 2행에 지급포인트/사용내역/잔여한도 등
  개별 열 이름이 바로 있어서, "1~2행 전체 병합"을 그대로 적용하면 그 개별 열 이름이 있을 자리가
  없어지는 문제가 있었음 — AskUserQuestion으로 확인한 결과 "열 이름은 유지하되 3행으로 내려서
  따로 두고, 그룹 제목은 1~2행을 병합"으로 확정. `tools/welfare-management.js`의
  `addPointsSheet` 수정: groupRow(1행, 팀명~근속연수 값 + 복지포인트/개인별복지사항 제목) →
  spacerRow(2행, 그룹 제목 병합 범위를 채우기 위한 빈 행) → labelRow(3행, 지급포인트/사용내역/
  잔여한도/장기근속(5주년)/장기근속(10주년)/41세검진/부모님검진 개별 열 이름) 3단 구조로 재구성.
  팀명~근속연수(1~5열)는 열 이름이 따로 없어서 1~3행 통째로 병합(`mergeCells(1,c,3,c)`), 그룹
  제목(F~H, I~L)은 1~2행만 병합(`mergeCells(1,6,2,8)` / `mergeCells(1,9,2,12)`). 데이터는
  자연히 4행부터 시작.
- 지급포인트(F열)/잔여한도(H열) 데이터 칸에 `numFmt: '#,##0"원"'` + `alignment: {horizontal:"right"}`
  적용(다른 칸은 기존처럼 가운데 정렬 유지). 웹 화면의 `formatWon`("1,000원" 표기) 관례와 일치시킴.
- **검증**: 이 파일은 브라우저 IIFE라 직접 실행은 안 되지만, 같은 로직을 scratchpad에 그대로
  복제해 Node + `vendor/exceljs.min.js`로 실행 — 병합 범위(A1~E1이 3행까지, F1이 2행까지 등),
  3행의 열 이름 값, 4행(첫 데이터 행) F/H열의 numFmt·정렬이 전부 의도대로 나오는 것 확인함.
  브라우저 미연결로 실제 다운로드 파일을 Excel에서 열어 화면으로 재확인은 못함 — 사용자가
  다운로드해서 확인 필요.

## 완료 (2026-08-05 — 대리점 칸 "+ 계약" 버튼 삭제)
- 사용자 요청 "비고옆에 +계약 버튼은 삭제" — 대리점명 세로 병합 칸(rowSpan으로 비고 행까지
  덮고 있어 시각적으로 비고 옆에도 걸쳐 보이던 칸) 안에 있던 "+ 계약" 버튼(`addContractInSpan`)을
  완전히 제거함. `appendRow`에서 버튼 생성 코드 삭제, `addContractInSpan` 함수 자체와 `appendRow`
  호출부의 인자도 같이 정리(죽은 코드로 안 남기고 삭제). `index.html`의 `.ac-add-contract-btn`
  CSS 규칙도 같이 삭제, `.ac-dealer-merged` 칸은 이제 입력칸 하나만 있음.
  - "+ 대리점 추가" 버튼(계약행 2개+이월금+총지급금액+비고행 세트 생성)은 그대로 유지 — 계약행이
    더 필요하면 이걸로 세트를 하나 더 만든 뒤 "이동" 버튼으로 원하는 블록에 끼워 넣으면 됨.
  - 바로 위 항목("+ 계약"으로 추가한 새 계약줄이 "비고"로 오분류되던 버그, `_rowKind:"normal"` 수정)은
    "+ 대리점 추가"가 만드는 계약행에도 여전히 적용되므로 그대로 유지.
- **검증**: `node --check` 통과. 브라우저 미연결로 화면에서 버튼이 실제로 사라졌는지는 확인 못함.

## 용어 정리 (2026-08-05, 사용자 지침)
- 대리점 1개 = **계약**(고객번호가 있는 정상행, 1개 이상) + **이월금** + **총지급금** + **비고**가
  한 세트("블록"). 계약이 추가되면 **계약줄**이 늘어나는 것이지 비고줄이 늘어나는 게 아님 — 이
  구분을 헷갈리면 안 됨(아래 항목이 실제로 이 구분이 깨져있던 버그).

## 완료 (2026-08-05 — 버그 수정: "+ 계약"으로 추가한 새 계약줄이 "비고"로 오분류되던 문제)
- 사용자 지적: "계약이 추가될 경우 계약줄이 추가되는거야. 비고줄이 추가되면 안됨." — 확인해보니
  `addContractInSpan`(대리점 칸 "+ 계약" 버튼)과 "+ 대리점 추가"가 만드는 새 계약행이 `blankRecord()`로
  만들어져 값이 하나도 없는 상태였는데, `rowType()`이 순전히 값 유무로 타입을 추론하는 구조라
  이 빈 계약행이 "blank"(비고)로 오판정되고 있었음 — 화면에도 "비고"로 보이고(사용자가 실제로
  본 증상), `hims-match.js`의 `buildBlocks`에서도 이 행이 블록 경계를 끊어버려서 그 앞뒤 정상
  계약행이 서로 다른 블록으로 갈라져 이월금/총지급금액 동기화가 잘못될 뻔한 부수 버그도 같이 있었음.
- **수정**: `rec._rowKind = "normal"` 표시(COLUMNS에 없는 이 도구 전용 필드, `closed`와 같은 패턴 —
  엑셀에는 안 나감)를 도입. `blankRecord(kind)`가 `kind==="normal"`일 때 이 표시를 붙이고,
  `rowType()`(agency-commission.js/hims-match.js 둘 다)이 값 판정보다 먼저 이 표시를 확인하도록
  수정 — 값이 비어있어도 "normal"로 확정됨. "+ 계약"(`addContractInSpan`)과 "+ 대리점 추가"가
  만드는 계약행 2개(`normalRec1`/`normalRec2`)에 적용. "+ 비고 행 추가"(`insertBlankAfter`)와
  "+ 대리점 추가"의 비고행(`blankRec`)은 표시를 안 붙여서 그대로 "blank" 유지.
- **검증**: scratchpad Node 테스트로 (a) `_rowKind:"normal"`인 빈 레코드가 양쪽 `rowType()` 모두에서
  "normal"로 나오는지, (b) 표시 없는 빈 레코드는 여전히 "blank"인지, (c) 계약1→새계약(빈,
  normal표시)→계약2→이월금→총지급금액→비고 순서 데이터에서 `hims-match.js`의 `buildBlocks`가
  끊기지 않고 정상행 3개를 한 블록(`normalIndices:[0,1,2]`)으로 묶는지 확인 — 전부 통과.
  브라우저 미연결로 실제 "+ 계약" 클릭 후 화면에 "비고"가 아니라 빈 계약행으로 보이는지는
  사용자 확인 필요.

## 완료 (2026-08-05 — 같은 패턴의 CSS 버그 재발견/수정: 대리점명 병합 칸도 표 왼쪽으로 밀려 보이던 문제)
- 사용자가 "대리점별 두번째 계약줄부터 왼쪽으로 밀려진 부분 수정해야해"라고 보고 — 바로 아래
  항목(2026-08-04)에서 이월금/총지급금액 병합 칸(`.ac-carrytotal-label-td`)에 났던 것과 똑같은
  원인이 `.ac-table td.ac-dealer-merged`(대리점명 세로 병합 칸, rowSpan으로 블록 전체를 덮음)에도
  남아있었음: `display:flex`가 `table-layout:fixed`인 표의 고정 컬럼 폭을 깨뜨려서, 이 rowSpan
  칸에 가려진 2번째 계약 줄부터(대리점 칸을 직접 안 그리고 rowSpan에 기대는 행들) 왼쪽으로 밀려
  보임. 당시(2026-08-04) 스크린샷으로는 "문제없어 보인다"고 판단해 손대지 않았던 부분인데,
  그 사이 "+ 계약" 버튼이 대리점 칸 안에 추가되면서(같은 날 다른 커밋) 내용이 늘어나 증상이
  실제로 드러난 것으로 보임.
- **수정**: `.ac-table td.ac-dealer-merged`에서 `display:flex`/`flex-direction`/`align-items`/`gap`
  전부 제거. 입력칸(`input`)과 "+ 계약" 버튼을 각각 `display:block; width:100%`로 줘서 flex 없이도
  세로로 쌓이도록 함(`.ac-add-contract-btn`도 `align-self` 대신 `display:block`+`margin-top`으로).
- **검증**: CSS만 수정, JS는 안 건드림. 브라우저 미연결로 화면 재확인은 못 함 — 사용자가 새로고침
  후 대리점별 2번째 계약줄부터 밀림 없이 보이는지 확인 필요.

## 완료 (2026-08-04 — 실제 발견된 CSS 버그: 이월금/총지급금액 병합 칸이 표 왼쪽 바깥으로 밀려 보이던 문제)
- 사용자가 계속 "밀림"/"정렬 안 맞음"이라고 보고했는데 데이터 파일도, 코드 로직도 다시 확인해봐도
  문제를 못 찾다가, 헤더 포함 스크린샷을 받고서야 정확한 원인을 발견함: **"총지급금액(실제지급달)
  계산서"** 텍스트가 대리점 칸보다도 왼쪽, 표 바깥 여백까지 밀려 나가 보이고 있었음(이월금/비고는
  괜찮았고 총지급금액만 문제였음 — 텍스트가 제일 길어서 증상이 뚜렷하게 드러난 것으로 보임).
- **원인**: `.ac-carrytotal-label-td`에 `display:flex`를 줬는데, 표 전체가
  `table-layout:fixed`(`.wf-table:not(.wf-leave-table)`)로 폭이 고정돼 있는 상태에서 `td`를
  flex 컨테이너로 바꾸면 테이블 레이아웃 규칙을 벗어나서, 셀 폭보다 긴 텍스트+버튼 조합이 셀
  경계를 무시하고 왼쪽으로 밀려 나가는 크롬 렌더링 특성이 있었음.
- **수정**: `display:flex`/`align-items`/`justify-content`/`gap`을 전부 제거하고 `text-align:center`만
  남겨서 일반 텍스트 정렬로 되돌림(`+ 비고 행 추가` 버튼은 `margin-left`로 텍스트 옆에 붙임).
  같은 패턴(`.ac-blank-note-td`, `.ac-move-cell`)은 flex를 안 썼거나(비고) 내용이 짧아서(이동
  버튼) 문제가 없었던 것으로 보여 그대로 둠. `.ac-dealer-merged`(대리점 세로 병합)는 세로
  방향(column) flex라 실제 스크린샷에서도 문제없이 잘 보여서 손대지 않음.
- **검증**: 스크린샷으로 실제 버그 재현 확인함(이월금 510,000 반영, 삼일제약/에이치플러스 분리
  등 다른 수정 사항은 전부 정상 작동 중인 것도 이 스크린샷으로 같이 확인됨 — 데이터/로직 자체는
  문제없었고 순수 CSS 버그였음). 수정 후 화면 재확인은 사용자가 새로고침 후 해야 함.
- **교훈**: "밀려 보인다"는 보고를 데이터 문제로 계속 추정해서 데이터 파일만 반복 대조했는데,
  실제로는 화면 렌더링 CSS 버그였음 — 다음에 비슷한 리포트가 오면 데이터 대조뿐 아니라 헤더 포함
  스크린샷을 더 일찍 요청해서 화면/데이터 중 어느 쪽 문제인지 먼저 구분할 것.

## 완료 (2026-08-04 — 이월금/총지급금액 병합 칸도 비고처럼 입력칸 없이 텍스트만 표시로 변경)
- "이월금을 고객번호 계약번호 상호 세칸만큼 병합 / 총지급금도 동일하게 병합 / 비고 처럼" —
  3칸 병합 자체는 이미 돼있었지만, 이월금/총지급금액 칸이 여전히 customerNo에 바인딩된 편집
  가능한 입력칸이었음(비고는 이미 입력칸 없는 순수 텍스트). "비고처럼"에 맞춰 이월금/총지급금액도
  입력칸을 없애고 `rec.customerNo` 값을 그냥 텍스트(span)로만 표시하도록 변경 — 부수 효과로
  `customerNo`가 `rowType()` 판정에 쓰이는 값이라 실수로 수정될 위험도 같이 없앰(비고 행 추가
  버튼은 그대로 유지, 텍스트 옆에 나란히 표시됨).
- **검증**: `node --check` 통과. 화면 확인은 브라우저 미연결로 못함.

## 완료 (2026-08-04 — "총지급금액에 비고 행 없는 블록" 대응: "+ 비고 행 추가" 버튼 신설)
- 사용자가 "윤현호 대리점 칸 비고까지 병합해줘" / "이일웅 비고란 병합표기"라고 요청 — 확인해보니
  이일웅 본부장/운현호 두 블록 다 총지급금액 행 다음에 비고(빈) 행 자체가 데이터에 없어서, 대리점
  세로 병합이 총지급금액에서 끊긴 채로 보이는 상황이었음(비고 행이 없으니 `buildBlocksForStyling`의
  `blankIndex`가 null → 병합 범위에 애초에 포함될 행이 없음). 브라우저 미연결이라 실제 데이터를
  직접 못 고치는 대신, 이런 블록을 자동으로 찾아서 한 번에 고칠 수 있는 기능을 화면에 추가함.
- `computeMissingBlankTotals`로 "총지급금액은 있는데 비고 행이 없는" 블록을 찾아서, 그 총지급금액
  행의 병합 칸에 **"+ 비고 행 추가"** 버튼을 표시하도록 함 — 누르면 바로 아래에 빈 행(`blankRecord()`)을
  삽입하고 다시 그림. 새로 생긴 행은 비어있으니 자동으로 "비고" 라벨로 표시되고, 그 블록의 대리점
  세로 병합에도 자동으로 포함됨(같은 세션에서 만든 `insertBlankAfter`가 `moveRow`와 마찬가지로
  표 전체 재렌더 방식이라 대리점 병합 등도 함께 재계산됨).
- **검증**: scratchpad 테스트로 이일웅(비고 없음)/박천홍(비고 있음) 두 블록을 섞은 데이터에서
  이일웅 블록의 총지급금액만 정확히 "비고 없음"으로 감지되는지 확인 — 통과.
- 사용자에게: 화면에서 이일웅/운현호 총지급금액 행 옆에 뜨는 "+ 비고 행 추가" 버튼을 누르면
  바로 해결됨(제가 직접 데이터를 못 건드리는 대신 만든 셀프서비스 버튼). 브라우저 미연결로
  직접 확인은 못함.

## 완료 (2026-08-04 — 실사용 스크린샷에서 대리점 병합 버그 발견/수정: 블록 경계를 넘어 다음 대리점을 흡수하던 문제)
- 사용자가 실제 화면 스크린샷(`화면 캡처 2026-08-04 153433.png`)을 보내줌 — "에이치플러스 양지"
  행이 대리점 칸 없이 통째로 한 칸씩 밀려 보이고 있었음(고객번호 300252가 원래 "대리점" 칸
  위치에 나타남). 원인 파악: 직전 커밋에서 만든 대리점 병합 로직이 "정상행에 새 대리점명이 있을
  때만 런을 끊는" 방식이라, 삼일제약-정세훈x 블록이 끝난(비고 행까지 지난) 뒤에도 다음 블록인
  에이치플러스 양지의 첫 행이 대리점명 없이 시작하니 그대로 삼일제약 rowspan에 흡수돼버려서
  대리점 칸 자체가 안 그려진 것이었음(대리점 칸이 없으니 그 아래 모든 셀이 한 칸씩 밀려 보임).
  이게 바로 "에이치플러스 양지병원~비고 부분 대리점 빈칸으로 병합하고 고객번호 라인 맞춰줘"와
  "이일웅 본부장 이월급,총지급금,비고까지만 포함" 요청의 실제 원인이었음(같은 버그).
- **수정**: `computeDealerMergeInfo`를 블록 경계(`buildBlocksForStyling`, 이월금/총지급금액/비고로
  구분되는 정상행 묶음) 기준으로 먼저 나누도록 재작성 — 절대 블록을 넘어 다음 블록까지 흡수하지
  않음. 블록 안에서만 대리점명이 바뀌는 지점 기준 서브 구간을 나누고(박천홍/이일웅처럼 한 블록에
  담당자가 섞인 경우 대비), 이월금/총지급금액/비고는 그 블록의 마지막 서브 구간에만 붙임.
  대리점명이 전혀 없는 블록(에이치플러스 양지처럼)은 블록 전체가 값이 빈 문자열인 하나의 병합
  칸으로 표시됨 — "대리점 빈칸으로 병합" 요건과도 정확히 맞음.
- **검증**: scratchpad 테스트로 스크린샷 상황을 그대로 재현하는 데이터(삼일제약 3계약 블록 +
  에이치플러스 1계약 블록)를 만들어 확인 — 에이치플러스 블록이 이제 삼일제약과 분리된 자기만의
  병합 칸(대리점명 빈칸)으로 나오는 것 확인. 기존에 검증했던 박천홍/이일웅(한 블록 내 대리점
  구분)과 대리점명 전혀 없는 블록(빈칸 유지) 시나리오도 재실행해서 회귀 없음을 확인함.
  브라우저 미연결로 실제 화면 재확인은 못함 — 사용자가 새로고침 후 에이치플러스 양지 줄이 이제
  제대로 정렬되는지 확인 필요.

## 완료 (2026-08-04 — 대리점 수수료 정산 화면: 대리점 구분 병합 수정 + 행 추가 2종 + 순서변경 + 이월금/총지급금액 라벨 병합)
- **대리점 병합 로직을 "블록 단위"에서 "연속된 같은 대리점명 단위"로 교체**: "박천홍 팀장건과
  이일웅 본부장건 구분해서 다시 표기해줘" — 원래 구현은 이월금/총지급금액으로 구분되는 블록
  전체를 한 대리점으로 보고 병합했는데, 실제로는 같은 이월금/총지급금액 구간 안에 서로 다른
  담당자(대리점명)가 섞여 있을 수 있어서 둘이 하나로 합쳐져 보이는 문제가 있었음. `computeDealerMergeInfo`를
  재작성해서, normal 행에 새 대리점명이 채워지는 지점마다 새 "런"을 시작하고(대리점명이 비어있는
  행이나 이월금/총지급금액/빈 행은 직전 런에 이어붙임) 대리점명이 바뀌는 순간 자동으로 구간이
  나뉘도록 함 — 블록 경계와 무관하게 실제 담당자 변경 지점을 기준으로 병합.
  - **부수 확인(사용자 질문)**: "대리점명 없는경우 빈칸유지" — 대리점명이 전혀 없는 새 블록(예:
    막 추가한 계약행)은 애초에 어떤 런에도 안 들어가서 병합 없이 개별 빈 입력칸으로 그대로
    남는다는 것을 scratchpad 테스트로 확인함(추가 코드 수정 불필요, 설계상 이미 그렇게 동작).
- **"행 추가" 버튼을 2개로 분리**: "+ 대리점 추가"(계약행 1개+이월금+총지급금액+비고행 4개를
  세트로 생성) / "+ 계약 추가"(계약행 1개만 표 맨 아래에 추가, 원하는 대리점 블록으로는 아래
  "이동" 버튼으로 옮기면 됨).
- **행 순서변경(이동) 기능 추가**: 모든 행에 ▲▼ 버튼("이동" 컬럼)을 추가해서 `data` 배열 안에서
  인접한 행과 자리를 바꿀 수 있게 함(맨 위/아래는 버튼 비활성화). 이동할 때마다 표 전체를
  다시 그려서 대리점 병합·블록 스타일 전부 재계산되도록 함.
- **이월금/총지급금액 행도 라벨을 3칸에 걸쳐 표시**: "이월금 뒤로 두셀까지 병합 / 총지급금액
  뒤로 두셀까지 병합 / 비고까지 셋다 중앙정렬" — 이월금·총지급금액 행의 고객번호 입력칸(값
  "이월금"/"총지급금액")을 고객번호/계약번호/상호 3칸에 걸쳐 표시하도록 병합(비고 행과 같은
  패턴). 비고 라벨의 정렬은 왼쪽→가운데로 변경, 이월금/총지급금액 라벨은 원래도 input 기본
  스타일이 가운데 정렬이라 추가 CSS 없이 이미 가운데였음 — 셋 다 가운데 정렬 요건 충족.
- **검증**: `node --check` 통과. scratchpad에 `computeDealerMergeInfo` 로직을 그대로 복제한
  테스트로 (a) 한 구간 안에 대리점 2명이 섞여도 각자 올바른 범위로 나뉘어 병합되는지, (b) 대리점명이
  전혀 없으면 병합이 아예 안 일어나는지(빈칸 유지) 확인 — 둘 다 통과. 행 추가 버튼/이동 버튼/
  이월금·총지급금액 병합 셀은 브라우저 미연결로 실제 화면에서 직접 확인 못함 — 사용자 확인 필요.

## 완료 (2026-08-04 — 총지급금액 아랫줄 병합 칸: 입력칸 없이 "비고" 텍스트만 표시하도록 단순화)
- 바로 위 항목에서 만든 병합 칸(고객번호/계약번호/상호 3칸 병합)이 편집 가능한 메모 입력칸
  (`note` 필드 바인딩)이었는데, 사용자가 "총지급금액 아래 비고 라고 텍스트만 하고 메모박스는
  삭제"라고 요청 — 입력칸을 없애고 `mergedTd.textContent = "비고"`로 고정 텍스트만 표시하도록
  단순화함. CSS도 input/textarea 대상 스타일에서 td 자체 스타일(회색 텍스트, 왼쪽 정렬)로 변경.
  `rowType()`에서 note를 blank 판정 제외 대상으로 뒀던 건 그대로 유지(원본 엑셀에 이미 들어있는
  비고 메모는 여전히 보존해야 하므로).
- **검증**: `node --check` 통과. 화면 확인은 여전히 브라우저 미연결로 못함.

## 완료 (2026-08-04 — 대리점 수수료 정산 화면: 대리점명 세로 병합, 총지급금액 아랫줄 비고화, 상태 필터/라벨 정리)
- **대리점명 세로 병합**: "대리점 명은 계약이 여러건이어도 통합해서 보여줘" → "대리점명은 첫줄부터
  ~이월금,총지급금,비고까지 병합해서 표기해줘"로 구체화됨. 블록(정상행들→이월금→총지급금액→
  비고행) 전체 범위를 `computeDealerMergeInfo`로 계산해서, 블록 첫 행에서만 `rowspan`으로 대리점
  칸을 한 번 그리고 나머지 소속 행은 대리점 칸 자체를 안 그리도록 `appendRow`를 재구성함.
  - 표시값은 블록 내 정상행 중 실제로 값이 채워진 첫 대리점명을 사용(원본 엑셀 병합 셀 특성상
    2번째 계약 행부터 대리점명이 비어있던 걸 자동으로 메꿔서 보여줌).
  - 이 병합 칸을 수정하면 블록에 속한 모든 행(정상행+이월금+총지급금액+비고행)의 `dealer` 필드에
    동시 반영·저장됨.
- **총지급금액 아랫줄(빈 행)을 비고 칸으로**: "총지급액 아랫줄 왼쪽 2-4번째 셀만 병합해서 비고로
  표기해줘" — 빈 행의 고객번호/계약번호/상호(왼쪽 2~4번째 칸, 대리점 칸은 위 병합에 이미 포함되니
  제외) 3칸을 하나로 합쳐 비고(`note` 필드) 입력칸으로 씀.
  - **`rowType()` 수정 필요했음**: `note`(비고)와 `dealer`(위 병합으로 값이 써짐) 필드가 채워져도
    여전히 "blank"(블록 경계)로 인식되도록 `agency-commission.js`/`hims-match.js` 양쪽의
    `rowType()`에서 이 두 필드를 "빈 행 판정" 대상에서 제외함 — 안 그러면 비고나 병합된 대리점명이
    들어간 순간 그 행이 "normal"로 오분류되어 블록 경계가 깨지고(HIMS 동기화 누적 계산 오귀속),
    종료 체크박스/상세 토글까지 잘못 나타날 뻔했음.
- **"표시" 필터 → "상태" 필터로 이름/기본값 변경**: 라벨 "표시:"→"상태:", 옵션 순서를
  유지/전체/종료로 바꾸고 기본 선택값을 "전체"에서 **"유지"**로 변경(종료 처리된 행은 평소엔
  안 보이게). 표 컬럼 헤더 "종료"도 "상태표시"로 이름 변경(체크박스 자체의 개별 라벨은 "종료" 유지).
- **검증**: `node --check` 통과 3개 파일. scratchpad Node 테스트로 (a) 비고/대리점명이 채워진 빈
  행이 여전히 "blank"로 분류되는지, (b) 실제 값이 있는 정상행은 여전히 "normal"로 잡히는지(오탐
  없음), (c) `buildBlocks`가 이런 빈 행이 끼어도 블록 경계를 올바르게 나누는지 확인 — 전부 통과.
  대리점명 rowspan 렌더링 자체(브라우저 DOM)는 이 세션에 브라우저가 연결 안 돼 직접 못 봄 —
  사용자가 화면에서 실제로 대리점 칸이 블록 전체에 걸쳐 한 번만 보이는지, 비고 병합 칸이 잘
  보이는지 확인 필요.

## 완료 (2026-08-04 — 대리점 수수료 정산 화면 후속 다듬기 3건: 종료/상세 범위, 블록 종료 전파, 미납/지연납 색상)
- **"종료"/"상세"는 고객행에만**: 이월금/총지급금액/빈 행에는 애초에 의미가 없는데도 모든 행에
  체크박스·토글 버튼이 붙어있었음 — `appendRow`에서 `rowType(rec) === "normal"`일 때만 종료
  체크박스·상세 토글 버튼·detailTr을 만들도록 제한(나머지 타입은 빈 `<td>`만 유지해 표 정렬은
  그대로). 삭제 버튼은 기존대로 모든 행에 유지.
- **블록 전체 종료 전파**: "해당 대리점의 모든 고객번호 종료시 이월금,총지급금,빈칸도 같이
  종료됨" 요청 — 화면(`agency-commission.js`)에 `buildBlocksForStyling`/`applyBlockClosedStyling`을
  추가해서, 한 블록의 정상행이 전부 종료 체크되면 그 블록의 이월금/총지급금액/빈 행도 같이
  회색 처리(`ac-row-closed`)되도록 함(종료 체크박스가 없는 행이라 rec.closed 자체는 못 쓰고
  블록 단위로 매번 다시 계산). 종료 체크박스 토글/행 추가/삭제/렌더 시점마다 재계산해서 반영.
  - 이 작업 중 서버 쪽(`hims-match.js`)에서 실제 버그가 될 뻔한 부분 발견: 블록의 정상행이 전부
    "종료"라 전부 건너뛰면 그 블록의 이월금/총지급금액 누적치가 0으로 남아 **기존에 들어있던
    값을 빈 문자열로 덮어쓸 뻔했음**. `blockClosedCount`로 블록별 종료행 개수를 세서, 정상행
    전부가 종료면 그 블록은 `closedBlocks`로 따로 보고만 하고 carryUpdates/totalUpdates에서
    완전히 제외(기존 값 그대로 보존)하도록 수정. 일부만 종료된 블록은 기존대로 활성 행 기준으로
    정상 계산(영향 없음). `server.js` 로그 메시지와 화면 `renderSyncResult`에도 "전체 종료돼
    건드리지 않은 블록" 건수 표시 추가.
- **미납/지연납 셀 색상**: "미납/수당금액일 경우 빨간색 폰트, 미납금 납부했을 경우 노란셀로
  표시" 요청 — 확인해보니 `.ac-unpaid` 클래스는 JS에서 토글만 되고 있었고 **CSS 규칙 자체가
  아예 없어서 미납 표시가 원래도 빨간색으로 안 보이고 있었음**(hims-match.js 주석엔 "빨간색으로
  표시"라고 적혀 있었지만 실제로 구현이 안 된 상태였던 기존 버그). `index.html`에
  `.ac-table input.ac-unpaid { color:#dc2626; font-weight:600; }` 추가. 늦게 입금된 경우
  ("6.30/48,000" 같은 날짜/금액 패턴)를 감지하는 `.ac-late-paid` 클래스도 새로 추가해
  `{ background:#fef9c3; }`(노란 셀)로 표시 — `applyUnpaidStyle`을 `applyCellStatusStyle`로
  확장해서 두 정규식(`/^미납\//`, `/^\d{1,2}\.\d{1,2}\/.+/`)을 함께 판정.
- **참고(휴내과 문의, 코드 변경 없음)**: 미납/지연납 칸에 슬래시 뒤 금액이 안 붙는 문제는
  코드 버그가 아니라 그 행의 月정산액(상세 패널) 필드가 비어있어서였음 — 사용자가 상세 패널에
  月정산액을 채워 넣으면 그 값을 그대로 이어붙이는 방식이라 자동으로 해결됨. 확인만 하고
  코드는 손대지 않음.
- **검증**: `node --check` 통과(agency-commission.js/hims-match.js/server.js). scratchpad
  Node 테스트로 (a) 블록 전체 종료 시 이월금/총지급금액이 아예 안 건드려지는지(빈 문자열로도
  안 덮임), (b) 부분 종료는 활성 행 기준으로 정상 계산되는지 확인 — 둘 다 통과. 브라우저 미연결로
  화면에서 실제 회색 전파·빨간/노란 셀 표시는 직접 확인 못함 — 사용자 확인 필요.

## 완료 (2026-08-04 — HIMS 동기화: 코아시스템즈처럼 한 고객번호에 계약이 2개인 경우 각각 확인되도록 수정)
- 위에서 계약번호 매칭을 없애고 고객번호만으로 매칭하도록 바꾼 뒤, 사용자가 "코아시스템즈처럼
  계약이 2개인경우 둘다 입금되었는지 확인"이라고 지적 — 같은 고객번호에 표 행이 2개(계약 2개)면
  그 달 HIMS 청구건도 2개인데 `feeRows.find()`가 매번 첫 번째 건만 찾아서 두 계약 행에 똑같이
  적용되는 버그가 될 뻔했음(하나는 완납, 하나는 미납이어도 둘 다 같은 값으로 잘못 채워짐).
- `hims-match.js`의 `computeSync` 매칭을 2단계로 수정: 같은 고객번호·같은 달 청구건이 여러 개면
  (1) 계약번호가 정확히 일치하는 건을 먼저 쓰고, (2) 없으면 아직 다른 표 행에 배정 안 된 건 중
  하나를 쓴다(`usedFeeRows` Set으로 이미 쓴 건 표시해 같은 청구건이 두 계약 행에 중복 반영되지
  않게 함). 계약번호가 표와 정확히 일치하면 여전히 그 방식이 우선이라, 원래 요청(계약번호 갱신
  때문에 매칭 실패하던 문제)도 그대로 해결된 채 다계약 고객도 각자 올바르게 확인됨.
- **검증**: scratchpad Node 테스트로 두 시나리오 확인 — (a) 계약번호가 정확히 일치하는 2계약
  고객: 각각 완납/미납이 올바르게 따로 반영됨, (b) 계약번호가 둘 다 표와 달라도(갱신 등) 청구건
  2개가 중복 없이 순서대로 각 행에 배정됨. 둘 다 통과.

## 완료 (2026-08-04 — 대리점 수수료 정산 화면: "전체/유지/종료" 표시 필터 추가)
- 앞선 "종료" 체크박스 기능에 이어서 사용자가 "전체/유지/종료 선택박스 기능 제공" 요청.
  `index.html`의 대리점 수수료 정산 표 위에 `#ac-filter-select`(전체/유지/종료) 드롭다운을 추가하고,
  `tools/agency-commission.js`에 필터 로직(`currentFilter`, `rowEls`, `matchesFilter`, `applyFilter`)을
  구현 — data 자체는 건드리지 않고 화면 표시(tr/detailTr의 display)만 필터링한다.
  - "상세" 토글 열림/닫힘 상태를 `tr.dataset.detailOpen`으로 별도 추적하도록 바꿔서, 필터로 숨겼다가
    다시 "유지"/"전체"로 보이게 했을 때 상세 그리드가 열려있던 행은 열린 채로 복원되도록 함.
  - 종료 체크박스 토글/행 추가/행 삭제 시점마다 `applyFilter()`를 다시 호출해 필터가 즉시 반영되게 함.
  - 메뉴 재진입(`init`) 때마다 `currentFilter`를 "all"로 초기화(드롭다운 기본값과 일치).
  - 참고: 이월금/총지급금액/빈 행은 `closed` 기본값이 항상 false라, "종료" 필터를 선택하면 그
    블록의 요약행도 함께 숨겨지고 해당 대리점의 종료 표시된 일반행만 보인다(요청 범위상 이렇게
    구현, 블록 요약행을 같이 보여줘야 한다면 추후 조정 필요).
- **검증**: `node --check` 통과. 브라우저 미연결로 실제 필터 동작(선택 시 행이 숨겨지는지, 상세
  열림 상태가 필터 전환 후에도 유지되는지)은 확인 못함 — 사용자가 화면에서 직접 확인 필요.

## 완료 (2026-08-04 — HIMS 동기화: 고객번호 단독매칭 + "종료" 행 별도관리(회색처리/동기화 제외))
- 사용자 요청 3건 처리:
  1. "동기화시에 계약번호 제외하고 고객번호로만 확인해줘" — `hims-match.js`의 `computeSync`에서
     HIMS 요금납부정보를 찾을 때 걸던 `contractNo` 일치 조건을 제거, 이제 고객번호+월만으로 매칭한다.
     (계약 갱신 등으로 표의 계약번호와 HIMS 계약번호가 달라져도 매칭되도록 하려는 목적으로 보임.
     같은 고객번호로 그 달 청구건이 여러 건이면 첫 매치를 쓰므로, 동시 보유 계약이 여러 개인
     고객이 실제로 있다면 다시 확인 필요 — 이번 요청 범위에선 고려하지 않음.)
  2. "바시스, 동서한방병원, 아이티센씨티에스, 인탑, 스냅컴퍼니 종료건은 별도관리할수있게 해줘" +
     3. "성신모텍 일부종료건은 그줄만 회색처리해줘" — 두 요청을 하나의 기능으로 통합 구현:
     각 행에 `closed`(종료) 불린 필드와 화면 체크박스("종료" 컬럼)를 새로 추가.
     - 체크된 행은 `.ac-row-closed` 클래스로 회색 처리(성신모텍처럼 회사 중 일부 행만 종료된
       경우는 이 체크박스를 사용자가 직접 켜면 됨)
     - `hims-match.js`의 `computeSync`와 `server.js`의 HIMS 조회 대상 목록 둘 다에서 `closed`
       행을 건너뛰도록 해서 동기화 대상에서 완전히 제외(블록의 이월금/총지급금액 완전동기화
       판정에도 영향 안 줌 — 있는 셈도 없는 셈도 안 치고 그냥 무시)
     - 위 5개 회사(바시스/동서한방병원/아이티센씨티에스/인탑/스냅컴퍼니)는 상호 전체가 종료된
       것으로 보고, 데이터를 불러올 때(`loadFromStorage`/업로드) `closed`가 아직 정해지지 않은
       행에 한해 자동으로 종료 표시되도록 `CLOSED_COMPANIES` 목록 + `applyClosedDefaults`로 처리
       (한 번 사용자가 체크/해제하면 그 값이 우선, 자동 판정으로 덮어쓰지 않음)
     - `closed`는 엑셀 COLUMNS에 없는 이 도구 전용 표시라 엑셀 다운로드에는 안 나가고, 파일을
       다시 업로드하면 위 5개 회사 외의 수동 종료 표시(예: 성신모텍 개별 행)는 초기화됨 —
       현재는 이 정도 범위로 구현, 필요시 나중에 보완
- **검증**: `node --check` 3개 파일(`agency-commission.js`/`hims-match.js`/`server.js`) 통과.
  scratchpad에 Node 유닛테스트 작성해 (a) 계약번호가 표·HIMS에서 달라도 고객번호만으로 매칭되는지,
  (b) `closed:true`인 행이 HIMS 조회/notFound 보고 없이 완전히 건너뛰어지는지 확인함(둘 다 통과).
  이 세션엔 브라우저 확장(Claude in Chrome)이 연결되지 않아 실제 화면에서 "종료" 체크박스 동작·
  회색 처리·5개 회사 자동 종료 표시는 직접 확인 못함 — **사용자가 index.html에서 대리점 수수료
  정산 메뉴를 열어 종료 체크박스가 보이는지, 체크 시 회색으로 바뀌는지, 저 5개 회사 행이 이미
  체크된 채로 보이는지 확인 필요**.

## 완료 (2026-08-04 — 버그 발견/수정: 대리점 수수료 정산 화면에 HIMS 동기화 버튼이 아예 없었음)
- 사용자가 실제로 "HIMS 동기화" 버튼을 누르려고 했는데 화면에 버튼이 안 보인다고 보고.
  `index.html`의 `agency-commission` 섹션 마크업을 확인해보니 `#ac-download-btn`/`#ac-dropzone`
  /`#ac-status`/`#ac-table-container`만 있고 `tools/agency-commission.js`가 이미
  `root.querySelector("#ac-sync-btn"/"#ac-sync-status"/"#ac-sync-result")`로 참조하는 엘리먼트
  자체가 HTML에 전혀 없었음(2026-07-28 HIMS 통합 때 JS 로직은 다 짜놓고 화면 버튼 추가를
  깜빡한 것으로 보임). `syncBtn.addEventListener(...)`가 `init()` 함수의 마지막 문장이라
  실행 시 `null.addEventListener` TypeError로 조용히 끝나서(콘솔에만 에러가 남고 다른 기능은
  멀쩡해 보여서) 지금까지 발견되지 않았던 것으로 추정.
  - **왜 이전 검증(2026-07-28)에서 못 잡았는지**: 그때 HIMS 동기화 검증은 `POST /hims/sync`→
    `GET /hims/status`를 scratchpad 테스트 스크립트로 직접 호출해 서버 쪽 API 계약만 확인한
    것이었고, 실제 `index.html` 화면에서 버튼을 눌러본 적은 없었음 — UI 자체를 실제로 클릭해서
    확인하는 과정이 빠져있었다는 교훈.
  - `index.html`에 `#ac-sync-btn`/`#ac-sync-status`/`#ac-sync-result`를 담은 새 카드를
    다운로드 카드와 표 사이에 추가. 설명 문구도 "1~6월만 관리"로 돼 있던 낡은 문구를
    "1~12월 관리(1~6월 확정값, 7~12월은 HIMS 동기화)"로 갱신(COLUMNS엔 이미 m7~m12가 있었는데
    설명만 안 따라간 상태였음).
- **검증**: Node로 `<script>` 블록 전체를 `new Function()`에 넣어 문법 오류 없는지 확인.
  이 세션엔 브라우저 도구가 없어 실제 클릭까지는 확인 못 함 — 사용자가 화면 새로고침 후
  버튼이 보이는지, 눌렀을 때 동기화가 정상 시작되는지 확인 필요.

## 완료 (2026-08-04 — HIMS 동기화: "총지급금액" 행 자동계산 + 7~12월만 대상으로 제한)
- 사용자가 hims.hilineisp.net 입금 확인 규칙을 다시 설명해줌: (1) 미납이면 "미납/수당금액" 표기
  (이미 구현돼있었음), (2) 거래처별 당월 합계는 "총지급금액" 셀에 넣되 미납액은 합계에서 제외
  (기존엔 전혀 계산 안 하고 있던 부분 — 이번에 추가), (3) "2.3/200,000"처럼 입금이 늦어진 경우
  그 금액을 실제 입금된 달의 이월금+총지급금액에 합산(이월금 쪽은 2026-07-23에 이미 구현돼있었고,
  이번에 총지급금액에도 같이 반영되도록 확장), (4) 원본 파일에 이미 1~6월이 채워져 있으니 동기화는
  7~12월만 대상으로 하고 1~6월은 조회·재계산 모두 건드리지 말 것(신규 요청).
- `tools/helper-server/lib/hims-match.js` 수정:
  - `SYNC_MONTHS = [7..12]` 상수 추가, 메인 루프와 이월금/총지급금액 마감 루프를 전부
    `for (let month=1;month<=12;...)`에서 `for (const month of SYNC_MONTHS)`로 교체.
    **이 수정이 없었으면 실제 버그로 이어질 뻔했음**: 기존 코드는 이월금 마감 루프에서 12개월
    전부에 대해 항상 `value: amount>0 ? ... : ""`를 push하고 있어서, 1~6월엔 애초에 이월 계산을
    안 하니 amount가 항상 0 → 기존에 수기로 입력돼있던 1~6월 이월금 값을 빈 문자열로 덮어써
    지워버렸을 것(총지급금액도 같은 패턴으로 만들었으면 동일 버그가 났을 것 — 처음부터 SYNC_MONTHS로
    제한해서 예방).
  - `buildBlocks`가 "총지급금액" 행도 인식하도록 확장 — 사용자 확인: 각 블록은
    "정상행들 → 이월금 → 총지급금액 → 빈행" 순서라 총지급금액 행은 바로 앞 이월금 행과 같은
    블록으로 묶어 `block.totalIndex`에 기록(중간에 정상행이 끼면 별개 블록으로 취급해 오귀속 방지).
  - `computeSync`에 `onTimeAcc`(그 달에 정상청구·그 달에 바로 완납된 금액의 블록별 월별 합) 신설.
    총지급금액[월] = `onTimeAcc[월] + carryAcc[월]`(그 달에 실제로 들어온 돈 전부, 미납은 애초에
    두 누적치 어디에도 더해지지 않아 자동으로 제외됨) — `totalUpdates` 배열로 반환.
    이월금과 동일하게 블록 전체가 HIMS 조회 성공했을 때만 계산(`blockFullySynced` 재사용).
  - 총지급금액 행을 찾지 못한 블록은 `blocksWithoutTotalRow`로 별도 보고(조용히 건너뛰기만 하면
    사용자가 원인을 알기 어려우므로).
- **완납 판정 기준 재확인(코드 변경 없음)**: 사용자가 "청구기준일 월==회사입금일 월이면 납부한
  것, 다르면 미납요금을 늦게 납부한 것"이라며 실제 사례(삼일제약, 고객번호 205663, 청구기준일
  2026.07.05/회사입금일 2026.07.30 — 둘 다 7월)를 제시. 완납/미납 판정을 "상태" 텍스트 대신
  회사입금일 존재 여부로만 할지 물어봤고, 사용자가 "상태=='완납' 조건 유지"를 선택함 — 즉 현재
  로직(상태가 정확히 '완납'일 때만 그 다음 월 비교로 온타임/지연을 가름)이 그대로 맞고, 위 사례도
  이미 구현된 동작과 일치(같은 달 → 온타임, m7=月정산액 그대로)함을 확인. 코드 수정 없음.
- `tools/agency-commission.js`/`tools/helper-server/server.js`: `job.result.totalUpdates` 적용 및
  동기화 결과 메시지(`renderSyncResult`, 서버 로그)에 총지급금액 반영 건수/총지급금액 행 누락 블록
  경고 추가.
- **검증**: 합성 데이터로 Node 유닛테스트 작성해 실행 확인(실제 HIMS 데이터는 이 세션에서 접근
  불가) — 온타임 입금, 지연 입금(이월+총지급금액 반영), 미납(합계 제외), 6월 이하 데이터 완전
  보존(조회 자체를 안 함), 총지급금액 계산값, 총지급금액 행 누락 감지까지 전부 통과. 테스트 파일은
  scratchpad에만 저장(저장소에 커밋 안 함). **다음에 실제 HIMS로 동기화 버튼을 눌러서 실데이터로도
  한 번 더 검증 필요** — 특히 "이월금 바로 다음이 총지급금액"이라는 행 순서 가정이 실제 파일과
  맞는지(사용자 확인으로 진행했지만 실물 파일 대조는 아직 못 함).

## 완료 (2026-08-04 — 대리점 수수료 정산 화면 표: 컬럼 축소 + 상세 접기)
- 사용자 피드백: 화면 표(브라우저, index.html)에서 컬럼이 24개나 돼서 입력칸 글씨(상호/고객번호 등)가
  잘려 보임. 이어서 "대리점명/고객번호/계약번호/상호와 날짜별 금액은 표시하고 나머지는 접었다
  펼 수 있게 해달라"는 요청으로 범위가 구체화됨.
- `tools/agency-commission.js`: `COLUMNS`(엑셀 다운로드/업로드용 전체 24필드, 순서 그대로 유지)와
  별개로 화면 표시용 `MAIN_KEYS`(대리점/고객번호/계약번호/상호 + 월별 m1~m12, 16개)를 새로 정의.
  나머지 8개(비고/개통일/계약일/계약만료일/계약종료/수익금액/매출액/月정산액)는 `DETAIL_COLUMNS`로
  분류해 각 행 아래 "상세" 토글로 펼치는 라벨+입력칸 그리드(`buildDetailRow`)에 넣음.
  - `cellInput`을 `buildFieldInput`(input/textarea 엘리먼트만 생성)과 `cellInput`(그걸 td로
    감싸는 래퍼)로 분리해서 메인 행 셀과 상세 그리드 필드 양쪽에서 재사용.
  - `applyRowType(tr, detailTr, rec)`로 시그니처 변경 — 이월금/총지급금액/빈행 배경색 클래스를
    메인 행과 상세 행 둘 다에 동기화해서 적용.
  - 메인 표는 `<colgroup>`으로 컬럼별 폭을 명시(대리점 100/고객번호 120/계약번호 100/상호
    170/월별금액 각 78px) — 기존 `.wf-table:not(.wf-leave-table)`의 `table-layout:fixed` +
    colgroup 패턴(복지제도 관리 표에서 이미 쓰던 방식)을 그대로 따름. 컬럼 수가 24→16으로
    줄고 폭도 넉넉해져서 글씨 잘림 문제 해결.
  - 각 입력칸에 `title` 속성(현재 값)을 추가해 혹시 더 긴 값이 들어와도 마우스 오버로 전체 텍스트
    확인 가능하게 함.
- `index.html`: 기존 `.ac-table td textarea`(비고칸 전용, min-width:140px 등) 규칙은 이제 비고가
  상세 그리드로 옮겨가면서 뜻하지 않게 상세 그리드 textarea까지 덮어쓰는 문제가 있어 삭제하고,
  `.ac-detail-grid`/`.ac-detail-field`/`.ac-detail-label` 등 새 그리드 전용 스타일로 교체.
- 엑셀 다운로드(`buildWorkbook`)·업로드(`parseWorkbook`)·HIMS 동기화 로직은 전혀 건드리지 않음 —
  화면 표시 방식만 바뀜.
- **검증**: `node --check`로 문법 확인만 완료. 이 세션엔 브라우저 확장(Claude in Chrome)이 연결되지
  않아 실제 화면 렌더링은 직접 확인 못 함 — 사용자가 index.html에서 "대리점 수수료 정산" 메뉴를
  열어 상세 토글/컬럼 폭이 의도대로 보이는지 확인 필요.

## 완료 (2026-08-03 — 프로젝트 git 저장소화 및 GitHub push)
- 그동안 git 저장소가 아니었던 `hiline/`을 git 저장소로 초기화하고 GitHub 원격 저장소
  (`https://github.com/heyjjoa-art/-hiline.git`, main 브랜치)에 처음으로 push함.
- `.gitignore` 신규 작성해서 다음은 커밋에서 제외함:
  - `node_modules/`, `*.log`, `server-log.txt`
  - **자격증명 파일**: `tools/helper-server/giro-config.json`(지로 로그인 정보),
    `tools/helper-server/mail-config.json`(메일플러그 앱 비밀번호) — `*.example.json`은 포함됨
  - `tools/helper-server/chrome-profile-*/`(로그인 세션이 든 브라우저 프로필, 용량도 큼)
  - `tools/helper-server/debug-fail-row46.png`(디버그 스크린샷)
- 이 저장소 전용 local git config로 `user.name=heyjjoa-art`, `user.email=heyjjoa@gmail.com`
  설정함(전역 git config는 원래 비어있었고 건드리지 않음).
- 첫 커밋 38개 파일 push 완료. `.claude/settings.local.json`은 사용자 PC의 전역 gitignore
  (`~/.config/git/ignore`)에 이미 등록돼 있어 자동으로 빠짐.
- **다음에 커밋할 때 참고**: 앞으로 작업할 때마다 별도 요청 없이 자동으로 커밋/push하지 않음 —
  사용자가 "깃푸쉬해줘"라고 명시적으로 요청할 때만 진행.

## 완료 (2026-07-31 — 세금계산서 양식수정: 비고란 고정 문구 자동 입력)
- 사용자 요청으로 "세금계산서 양식수정" 변환 시 후양식(대량등록 양식 정발행) 전체 비고란(59번
  컬럼 — 품목별 비고1~4(30/38/46/54)와 별개인 전체 비고)에 항상
  "입금계좌 - 국민은행 519701-01-220702" 문구가 채워지도록 `tools/tax-invoice-convert.js` 수정.
  기존엔 "규칙에 없어 공란" 처리였던 필드라 `DST.remark: 59` 추가 + `convertRow`에서
  무조건 `REMARK_TEXT` 설정하도록 변경.
- 어느 컬럼이 "전체 비고"인지는 템플릿 3번째 행(개별 필드 헤더)을 Node로 직접 읽어서 확인함
  (54번까지는 품목4까지의 비고/날짜/품목 필드, 55~58은 현금/수표/어음/외상미수금, 59번이
  "비고\n(길이:100)"으로 전체 비고).
- 실제 샘플(`etc/2026.07.15 세금계산서 발행건.xlsx`) 203건 전체를 Node로 재변환해서 59번 컬럼에
  문구가 빠짐없이 들어가는 것 확인함.

## 완료 (2026-07-31 — 지로납부번호.xlsx 중복 데이터 삭제)
- 사용자가 "KT 25일자 99,330원 내역이 말일자와 중복"이라고 알려줘서 `etc/지로납부번호.xlsx`를
  직접 확인 — 30행("25일자", KT, 99330원, 전자납부번호 32013384907)과 46행("말일자", KT,
  99330원, 전자납부번호 32013384907)이 같은 전자납부번호로 완전히 중복된 것 확인.
  25일자 쪽(30행)을 삭제함(ExcelJS로 직접 편집 — 이 PC엔 Python이 없어서 xlsx 스킬의
  openpyxl 대신 프로젝트에 이미 있는 ExcelJS/Node 방식 사용). 삭제 후 46행으로 줄었고,
  KT 관련 행 재확인해서 말일자 99,330원 건만 남아있는 것 확인함.
- 참고: `etc/`는 원래 "건드리지 않는" 샘플 파일 취급이지만, `지로납부번호.xlsx`는 지로
  자동기안이 실제로 읽는 라이브 데이터 파일이라 예외 — 과거(2026-07-20)에도 사용자가
  직접 수정한 전례 있음(당시는 26회 거래처 빈칸 forward-fill 오류).

## 완료 (2026-07-28 — 도우미 서버 3개 → 1개로 통합)
- 사용자 요청("메뉴별로 도우미서버가 여러개인데 하나로 통합할 수는 없어?")으로 지로 자동기안
  (포트 8787) / 메일 수신함(포트 8788) / HIMS 동기화(포트 8790) 세 개의 독립 Node 로컬 도우미
  서버를 `tools/helper-server/` 하나로 합침. 포트는 8787 하나만 쓰고, 기능은 경로로 구분
  (`/giro/*`, `/mail/*`, `/hims/*`). Windows 시작프로그램 등록도 `hiline-helper-server.vbs`
  하나로 통일(기존 `giro-auto-draft-helper.vbs`/`mail-inbox-helper.vbs`는 제거,
  hims는 애초에 시작프로그램 등록이 없었음).
  - **조사 단계에서 hims-payment-sync가 사실상 미완성 상태였음을 발견**: `node_modules`/
    `package-lock.json`/`README.md`/`start-server-now.bat`/`install-startup.js`가 전부
    없었고(`agency-commission.js`는 없는 `start-server-now.bat`을 안내하고 있었음), `package.json`엔
    `main: "run.js"`인데 그 파일도 없었음. 이번 통합으로 자연스럽게 함께 해결됨(unified
    `node_modules`에 playwright 포함, `README.md`/`start-server-now.bat`/`install-startup.js`
    전부 갖춤).
  - **디렉터리 재구성**: `chrome-profile`(지로)/`chrome-profile`(HIMS, 이름 같지만 별개 폴더)을
    각각 `chrome-profile-giro`/`chrome-profile-hims`로 이름 붙여 한 곳에 공존시킴 — 같은 드라이브
    내 `mv`(rename)라 로그인 세션 손실 없이 그대로 이전됨(재로그인 불필요, 사이트 계정 잠김
    위험이 있어 특히 신경 씀). `node_modules`도 giro 것(playwright/exceljs, 52MB)을 베이스로
    mail 것(imapflow 등, 6.9MB)을 병합(패키지명 겹치는 것 없어 충돌 없음) — 네트워크 필요한
    `npm install` 없이 기존 설치를 그대로 재사용.
  - **코드 정리**: 지로/HIMS의 `uncaughtException`/`unhandledRejection` 방어 코드(2026-07-20
    사고 이후 추가됐던 것)를 서버 전체에 공용 적용 — 메일 서버는 원래 이 방어가 없었는데
    통합하면서 동일하게 보호됨. HIMS의 `rowType`/`toHimsCustNo`가 `server.js`와
    `lib/hims-match.js`에 중복 정의돼 있던 것도 `lib/hims-match.js` 하나로 정리.
  - **실사용 중 실제 버그 2건 추가 발견 및 수정** (어제 "실행 방법" 카드 삭제의 후속 여파 —
    당시 "JS는 null 체크가 있어 안전하다"고 판단했던 게 틀렸음): `tools/giro-draft-preview.js`의
    `initGiroDraftTool`이 삭제된 `#gd-open-site-btn`에 **조건 없이** `addEventListener`를 걸고
    있어서 지로 메뉴를 열자마자 `TypeError`가 나서 전체 초기화가 멈추는 상태였음(가장 심각).
    `renderFromCache`의 `cmdEl.textContent = ...`도 `if (target)`일 때마다 같은 이유로 터지는
    상태였음. 둘 다 삭제된 UI 잔재라 관련 코드 전부 제거해서 해결.
  - 프론트엔드 3개 파일(`tools/giro-draft-preview.js`, `tools/mail-inbox-preview.js`,
    `tools/agency-commission.js`) 전부 새 포트(8787 통일)/새 경로로 갱신, `index.html`의 메일
    안내 카드도 `tools/helper-server/` 경로로 갱신.
  - **검증**: 새 `server.js`와 lib 6개 파일 전부 `node --check` 통과. 실제로 띄워서
    `GET /giro/status`, `GET /hims/status`, `OPTIONS /giro/run`(204), 존재하지 않는 경로(404)
    확인, `GET /mail/messages`는 실제 메일플러그 계정으로 조회까지 성공(최신 메일 3건 정상
    반환). 지로/HIMS의 `POST /run`, `/sync`(실제 Chrome 자동화 트리거)는 이 시점엔 직접 실행하지
    않았음 — HIMS 동기화는 바로 이어서(아래 항목) 실사용 검증함.
  - 기존 세 디렉터리(`tools/giro-auto-draft/`, `tools/mail-inbox/`, `tools/hims-payment-sync/`)는
    삭제함(디버그용 PNG 11개 등 스크래치 파일 포함, 전부 새 위치로 이전 완료된 뒤라 정보 손실
    없음). 자세한 엔드포인트/설정 방법은 `tools/helper-server/README.md` 참고.
  - 통합 서버를 시작프로그램에 등록하고 지금 세션에서도 실행해둠 — 재부팅 전까지 바로 사용 가능.

- **HIMS 동기화 실사용 검증 완료 (같은 날 이어서)**: 사용자 요청으로 통합 후 처음으로 HIMS 동기화를
  실제 로그인 세션 + 실제 고객 데이터로 3단계 테스트함.
  1. `launch-chrome-hims.bat`으로 전용 Chrome을 띄우고 사용자가 직접 로그인 → `lib/hims-scrape.js`의
     `fetchFeeRows()`를 실제 고객(`HI300928`)에 대해 호출해서 계약번호/청구월/완납상태/회사입금일
     등 2건 정상 조회됨을 확인(scratchpad에 임시 테스트 스크립트 작성해서 확인, 저장소엔 안 남김).
  2. 그 실제 데이터로 `lib/hims-match.js`의 `computeSync()`를 돌려서, 2024년 7월 완납 청구건이
     `m7` 칸에 올바른 금액(`10,000`)으로 매칭되는 것 확인 — 매칭 로직도 실데이터 기준 정상.
  3. 실제 `POST /hims/sync` → `GET /hims/status` 폴링까지 서버 API 전체 경로로 재현 —
     `connecting → fetching → computing → done` 상태 전환 정상, 로그 메시지 정상, 결과값도
     2단계와 동일하게 나옴.
  - 스크래핑/매칭/HTTP API 세 계층 모두 실데이터로 검증된 것은 이번이 처음(구
    `hims-payment-sync`는 완성도가 낮아 이 정도 검증 기록이 없었음, 위 통합 항목 참고).
  - **주의할 점 발견**: HIMS 로그인 세션이 Chrome 창을 닫으면 금방 풀리는 것으로 보임 — 이번
    세션에서 두 번 다 `launch-chrome-hims.bat`을 새로 실행하니 매번 로그인 페이지로 돌아갔음
    (세션 쿠키 만료가 짧거나 브라우저 종료 시 세션이 끊기는 사이트일 가능성). 실사용에서
    "동기화" 버튼을 눌렀는데 방금 로그인했는데도 또 로그인해야 하는 패턴이 반복되면 이 부분을
    더 조사해볼 것.
  - 테스트 종료 시점에도 HIMS Chrome 창은 로그인 상태 그대로 열려있음(`browser.close()`는 CDP
    연결만 끊고 실제 창은 안 닫는다는 기존 설계가 테스트 스크립트에서도 그대로 확인됨).

## 완료 (2026-07-28 — 지로 메뉴 화면 정리, 위 통합 작업 때 일부 오류 발견/수정됨)
- `index.html`의 "지로청구서 자동기안" 섹션에서 "실행 방법" 카드와 그 아래 "로그인 정보",
  "처리 범위" 카드를 사용자 요청으로 삭제함. "기안 대상 확인" 카드만 남음(날짜 선택/자동 기안
  시작 버튼/미등록 건 재등록/엑셀 재업로드 기능은 그대로 유지).
  - ~~삭제된 카드 안에 있던 `#gd-open-site-btn`/`#gd-cmd`는 JS에서 null-safe하게 처리돼
    문제없다고 판단했었으나, 실제로는 `openBtn.addEventListener(...)`가 조건 없이 호출되고
    있어서 런타임 에러가 나는 버그였음~~ — 위 "도우미 서버 통합" 작업 때 발견해서 수정함.

## 완료 (2026-07-22 — 메일 수신함 메뉴 추가)
- 인덱스 메뉴 최상단에 "메일" 메뉴 추가 — `eunhye@hilineisp.net` 수신함(보낸사람/제목/받은날짜/
  읽음여부)을 조회하는 기능. 지로 자동기안과 동일하게 브라우저는 외부 계정 인증을 직접 다룰 수
  없어(IMAP 소켓 연결도 불가) `tools/mail-inbox/`에 독립 Node.js 로컬 도우미 서버(`server.js`,
  포트 8788, `imapflow` 사용)를 만들고 `index.html`은 `GET /messages`를 호출해 목록만 표시
  (`tools/mail-inbox-preview.js`). `start-server-now.bat`/`install-startup.js`도 지로 도구와
  동일 패턴으로 추가.
- **인증 방식 결정**: 사용자가 처음에 구글 계정 로그인 비밀번호를 대화창에 평문으로 여러 번
  붙여넣음 → 그 비밀번호는 어디에도 저장하지 않았고, 대신 "IMAP + 앱 비밀번호" 방식을 사용자가
  직접 선택함(OAuth/Playwright 로그인 자동화 대신). **사용자에게 대화창에 노출된 기존
  비밀번호(`Happy0130!!`)는 변경 권장 안내함.**
- **중요한 바로잡음: 구글 계정이 아니라 메일플러그(MailPlug)였음** — 처음엔 `eunhye@hilineisp.net`을
  구글 워크스페이스로 가정하고 `imap.gmail.com`으로 접속했으나 `a1 NO Lookup failed <id>` 응답을
  받음(비밀번호 확인 전 단계에서 실패 = 계정 자체를 못 찾음). 사용자 질문("구글 계정이 아닌 그냥
  메일을 소팅할 수는 있냐")을 계기로 도메인 MX/SPF 레코드를 직접 조회함
  (`nslookup -type=MX hilineisp.net` → `securemx01/02.msgsys.net`, TXT의 SPF에
  `include:mailplug.com`) — 실제 메일 서비스가 **메일플러그**임을 확인. `server.js`의 기본
  IMAP 호스트를 `imap.mailplug.co.kr:993`로 수정, `mail-config.example.json`/`README.md`/
  `index.html` 안내 문구도 전부 메일플러그 기준으로 갱신함. 이 서버로 raw TLS 접속해 배너
  (`* OK IMAP4 ready`) 확인 완료. 자세한 진단 과정은 `tools/mail-inbox/README.md` 하단 기록 참고.
- **완료 — 실제 계정으로 end-to-end 조회 성공 (2026-07-22)**: 사용자가 메일플러그에서 POP3/IMAP을
  켜고 앱 비밀번호를 발급받아 `mail-config.json`을 채움. 처음엔 `GET /messages` 요청이 응답 없이
  계속 멈추는 문제가 있었음(curl 45초 타임아웃에도 0바이트).
  - **원인**: `imapflow`가 기본으로 `AUTHENTICATE PLAIN`(SASL) 방식을 먼저 시도하는데, 메일플러그
    서버는 이 커맨드 자체를 `"2 BAD invalid command"`로 거부함(원인 파악은 raw TLS 소켓으로 직접
    `LOGIN`/`SELECT`/`FETCH`를 보내보며 확인 — 옛날 방식 `LOGIN "id" "pw"` 커맨드는 즉시
    성공했는데, imapflow 기본 설정으로는 SASL 시도 단계에서 멈춰버렸음).
  - **수정**: `server.js`의 `ImapFlow` 생성 옵션에 `auth.loginMethod: 'LOGIN'`을 추가해 SASL을
    건너뛰고 고전 LOGIN 커맨드를 강제로 쓰도록 함. 이후 실제 계정으로 `GET /messages` 호출 →
    실제 받은 메일 3건(제목/보낸사람/날짜/읽음여부) 정상 반환 확인함.
  - **자동 시작 등록 완료**: `node install-startup.js` 실행 → Windows 시작프로그램 폴더에
    `mail-inbox-helper.vbs` 생성됨(`%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\`).
    다음 Windows 로그인부터 콘솔 창 없이 자동으로 서버가 켜짐. 지금 이 세션에서도
    `node server.js`를 수동으로 띄워둔 상태라 재부팅 전에도 바로 사용 가능.

## 완료 (2026-07-20 — 지로청구서 자동기안 실제 동작 완성)
- **Step01~04 실제 DOM recon 완료 및 run.js에 반영**: 이전까지는 스크린샷만 보고 추정한 셀렉터였는데,
  `launch-chrome.bat`으로 띄운 실제 브라우저에 `connectOverCDP`로 붙어서 일반지로요금/전기요금·수신료/
  KT통신요금 세 메뉴 전부 실제 DOM(입력창 id, 버튼 class, GoUrl 메뉴 코드)을 확인하고 코드로 옮김.
  - 일반지로요금(Step01~04): `#gironum`+`#divGiroSearchButton`+"다음" → `#num`+`.btn_ty8_03`(조회) →
    "상세보기" → "기안하기"/"계속 기안하기"
  - 전기요금/수신료(Step01~03, GoUrl `mPayKepco`): `#number_2`(고객번호) → 상세보기 → 기안
  - KT통신요금(Step01~03, GoUrl `mPayKtcomm`): `#number`(전자납부번호, 라디오 기본값) → 상세보기 → 기안
  - 이미 기안 완료된 건은 "상세보기" 버튼이 사라지는 것으로 감지해서 중복 기안 없이 자동 스킵
  - "조회" 같은 흔한 텍스트가 상단 메뉴("내역조회" 등)와 겹쳐 엉뚱한 링크를 클릭한 사고가 있었음 →
    실제 확인된 class(`.btn_ty8_03`)로만 찾도록 수정
  - 25일자 실 데이터로 지로 4건(한국무역정보통신 726,000/550,000/550,000/495,000원) 전체 파이프라인
    실제 기안까지 성공 검증, 이어서 전기요금 1건(한국전력 477,280원)·KT 2건(99,330/13,480원)도
    실제 기안 성공 검증 — "기안문서 관리" 화면에서 7건 전부 확인함
  - **엑셀 데이터 오류 발견 및 수정**: `지로납부번호.xlsx` 25일자 29행이 거래처 칸이 비어있어
    forward-fill로 "전기요금"을 잘못 이어받고 있었음(실제로는 KT, 말일자 45행에 동일 번호가
    KT로 명시돼있어 확인). 원본 엑셀을 사용자가 직접 수정함.
- **코드 리팩터링**: 핵심 로직을 `tools/giro-auto-draft/lib/draft-runner.js`(Chrome 연결/로그인 대기/
  타입별 메뉴 이동·입력·기안)로 분리해서 `run.js`(CLI)와 `server.js`(아래) 둘 다 공용으로 사용하도록 함.
  `lib/launch-chrome.js`는 `launch-chrome.bat`과 동일한 동작을 코드로 재구현(서버가 버튼 클릭만으로
  Chrome을 띄우기 위함).
- **index.html에서 원클릭 실행 기능 추가**: 브라우저는 Chrome 프로세스를 직접 실행할 수 없어서,
  로컬 도우미 서버(`tools/giro-auto-draft/server.js`, 포트 8787)를 새로 만듦 — index.html이
  `POST /run`으로 날짜를 보내면 서버가 Chrome 실행→로그인 대기→기안까지 전부 처리하고,
  `GET /status`를 폴링해서 진행 로그를 화면에 실시간 표시(`tools/giro-draft-preview.js`에 추가).
  - `install-startup.js`로 Windows 시작프로그램에 서버를 숨김 실행 등록 완료(다음 로그인부터 자동 실행,
    지금 세션에도 수동으로 띄워서 즉시 사용 가능). `start-server-now.bat`은 즉시 수동 실행용.
  - Playwright로 index.html을 실제로 열어 메뉴 클릭→날짜 선택→버튼 클릭→서버 호출→진행 로그 표시까지
    엔드투엔드 검증(이미 기안 완료된 25일자 7건으로 안전하게 테스트, 전부 "이미 기안 완료"로 정상 스킵됨).
  - 작업 종료 시(성공/실패 무관) 항상 "기안문서 관리" 화면으로 자동 이동하도록 마무리 단계 추가.
  - 미리보기 목록(`tools/giro-draft-preview.js`)의 "자동 기안 대상"/"수동" 배지가 지로번호만 자동으로
    표시하던 것을 고객번호(전기요금)/전자납부번호(KT)도 자동으로 표시하도록 수정.
  - **버그 수정**: 서버가 같은 프로세스에서 두 번째 작업을 처리한 뒤 Playwright 다이얼로그 이벤트
    경합(`ProtocolError: No dialog is showing`)으로 통째로 크래시되는 문제 발견 → `runForDate()` 끝에
    `browser.close()`(CDP 연결만 해제, 사용자의 실제 Chrome 창은 안 닫힘)를 항상 실행하도록 finally
    추가. 추가로 `server.js`에 `process.on('uncaughtException'/'unhandledRejection')`을 걸어서 앞으로
    비슷한 예기치 못한 오류가 나도 서버 프로세스 자체는 죽지 않고 해당 작업만 오류 처리되도록 함.
  - 말일자 KT 항목도 날짜와 무관한 타입 기반 처리라 별도 코드 수정 없이 이미 지원됨을 확인.
- **정리**: recon 과정에서 만든 디버그 스크래치 파일(`diagnose1~29.js`, `step*.html`, `kepco-step*.html`,
  `kt-step*.html`, `menu.html`, `giandoc.html` 등) 전부 삭제 — 전부 `lib/draft-runner.js`에 실제
  셀렉터로 반영되고 동작 검증까지 끝난 상태라 정보 손실 없음.
- `README.md`를 새 구조(원클릭 실행이 기본, 터미널 실행은 문제진단용)에 맞게 갱신함.

- **"미등록 건" 감지 및 선택 재등록 기능 추가**: 실제 말일자 16건(LG유플러스 9건+KT 7건) 운영
  실행에서 스크립트 로그는 "완료"였는데 실제로는 등록 안 된 건이 있었던 사례가 있어서, try/catch
  기반 성공/실패 판정을 더 이상 신뢰하지 않기로 함 — 대신 작업이 끝나면 항상 "기안문서 관리"
  화면을 실제로 읽어서(`fetchRegisteredIdentifiers`, 지로는 전자납부번호/전기·KT는 표시된 번호로
  대조) 각 건의 실제 등록 여부를 확인하고, 확인 안 된 건만 `unregistered`로 반환하도록
  `lib/draft-runner.js`를 수정함 (이 판정이 "이미 기안 완료로 스킵된 건은 사실 등록된 것"인
  경우도 올바르게 잡아줌).
  - `runForDate`에 `onlyRows`(엑셀 행번호 배열) 옵션 추가 — 특정 행들만 골라서 재처리 가능.
  - `server.js` `POST /run`이 `onlyRows`를 받아 그대로 전달하도록 수정.
  - index.html에 "미등록 건" 빨간 박스 + "미등록 건만 다시 등록" 버튼 추가
    (`tools/giro-draft-preview.js`), 클릭 시 마지막 작업의 날짜+미등록 행번호로 재실행 요청.
    Playwright로 서버 응답을 가짜로 만들어(mock) UI 렌더링과 재등록 요청 바디까지 검증함.
  - **버그 발견 및 수정 (실사용 중, 2026-07-21)**: 사용자가 실제 말일자 16건을 index.html 버튼으로
    기안한 뒤 미등록 6건이 표시돼서 재확인 요청 → 실제로는 6건 다 정상 등록된 상태였는데
    "기안문서 관리" 목록이 **페이지네이션**(1페이지 10건 + 2페이지 6건, `goPage('N')`)이 있어서
    `fetchRegisteredIdentifiers`가 1페이지만 읽어 뒤 페이지 건들을 미등록으로 오판한 것이었음.
    페이지 링크(`a[onclick*="goPage"]`)를 찾아 끝까지 순회하며 모으도록 수정, 같은 6건으로
    재검증해서 "등록 확인 6건/미등록 0건"으로 정상화됨을 확인함.

- **실사고 발생: 말일자 16건이 실제로 "상신"됨 (2026-07-21)**: 사용자가 미등록 건 확인 중 기안문서
  관리 화면에서 직접 "상신" 버튼을 눌러 16건(엘지유플러스 9건, KT 7건, 총 4,436,750원)이 실제
  접수됨. **자동화 코드는 "기안하기"/"계속 기안하기"만 클릭하고 상신은 절대 클릭하지 않음**(코드
  확인함) — 사용자의 수동 클릭이었던 것으로 보임. 읽기전용으로 사이트를 확인한 결과:
  - 기안자(권은혜)와 승인자(정연경)가 별도 계정이고, 상신된 자료를 취소(회수)하는 기능은
    기안자 화면 어디에도 없었음("진행상황 보기"는 조회만, "기안자 결재함"은 승인자가 반려한
    건만 보이는 곳이라 비어있었음) — 취소 권한은 승인자 계정에만 있는 것으로 추정.
  - 다행히 사이트 설계상 "상신된 자료를 당일 중 납부하지 않으면 자동으로 사라짐" — 승인자가
    오늘 승인/납부 처리를 안 하면 자동으로 무효화됨. 사용자에게 정연경님께 직접 연락해서
    오늘 이 건들을 승인/납부하지 말아달라고 요청하도록 안내함.
  - **재발 방지 1**: "당일 상신하지 않으면 기안 자료가 사라지니 주의해주세요"라는 기존 안내 문구가
    "상신을 해야 한다"로 오해되기 쉬웠던 것으로 보여, `lib/draft-runner.js`의 실행 종료 로그와
    `index.html`의 "처리 범위" 설명을 "상신은 실제 접수 행위라 취소가 어려우니(승인자 계정에서만
    가능), 정말 납부 준비가 됐을 때만 직접 상신하고, 테스트/재확인 목적이면 상신하지 말고 그대로
    둬서 자동 소멸시키는 게 안전하다"는 취지로 명확하게 고침.
  - 사용자는 이후 수동으로 상신 클릭한 적 없다고 확인함 — 정확한 원인은 미확정(코드에는 상신 클릭이
    없음을 재확인했으나, 과거 캡처에 "단독 승인자의 경우 '기안하기'가 바로 승인자결재함으로
    이동된다"는 안내문구가 있어 계정 설정과 관련됐을 가능성을 의심 중, 추후 안전하게 확인 필요).
  - **재발 방지 2 (안전장치 추가)**: 배치의 **첫 번째 건만 처리한 직후** "기안문서 관리"에 대기
    상태로 남아있는지 확인하고, 사라져 있으면(상신/처리로 넘어간 것으로 추정) 나머지 항목을
    처리하지 않고 즉시 중단하도록 `runForDate`에 안전핀을 추가함(`abortedEarly` 필드로 반환).
    `index.html`에 빨간 경고 배너(`#gd-abort-warning`)를 추가해 중단 시 눈에 띄게 표시.
    Playwright로 mock 데이터를 이용해 UI 표시까지 검증함. 다음 실행부터 재발 시 최대 1건에서
    멈추도록 방지됨 — 사용자가 내일(2026-07-22) 말일자로 재테스트 예정, 결과 확인 필요.

## 완료 (이전)
- 기존 업무 투두리스트 프로젝트(`todo.html`) 삭제, CLAUDE.md를 엑셀 자동화 도구 프로젝트로 초기화
- 프로젝트 기본틀 생성: `index.html`(메뉴형 홈페이지 허브), `tools/`(변환 로직), `vendor/`(SheetJS), `handoff.md`
- **첫 기능 "세금계산서 양식수정" 구현 완료**
  - 전양식(발행건 xlsx) → 후양식(대량등록 양식 정발행) 변환, 드래그&드롭 업로드 + 다운로드
  - 처리는 전부 브라우저 안에서 SheetJS로 수행 (Python 미설치 환경이라 서버/설치 없이 동작하도록 결정)
  - 구현 파일: `tools/tax-invoice-convert.js`, `tools/templates/대량등록_양식_정발행_template.xlsx`(양식 원본 복사본), `vendor/xlsx.full.min.js`
  - 사용자가 정한 매핑 규칙(9개 항목) + "규칙에 없는 항목은 공란으로 둔다"는 방침 반영:
    - 등록번호 하이픈 제거, 이메일/연락처/휴대폰 순서 교정, 공급가액+세액=합계액(숫자), 작성일자 yyyymmdd,
      과세/사업자 고정값, 청구(C)/영수(R) 텍스트 변환, 품목1~4 매핑 + 내용 있을 때만 품목일자=작성일자
    - 종사업장번호, 담당자2 정보, 비고(전체/품목별), 현금·수표·어음·외상미수금, 책번호/일련번호는 공란 유지
  - Node로 실제 샘플 데이터(`etc/2026.07.15 세금계산서 발행건.xlsx`) row4~6 변환 결과를 직접 검증함 — 모든 필드 정상

- **정답 파일 대조 검증 완료**: 사용자가 직접 수기로 만든 `etc/대량등록 양식 (수기수정).xlsx`(203행)와
  변환 결과를 Node로 전수 비교. 차이는 H열(등록번호) 하이픈 처리 방식 단 1건뿐이었고, 그 외 모든 행·모든 열
  (상호/주소/이메일 순서/합계액/작성일자/청구·영수/품목1~4) 100% 일치.
  사용자에게 확인한 결과 하이픈 제거(원래 1번 규칙)가 맞는 것으로 결론 — 코드 변경 없음, 수기수정 파일 쪽이 규칙과 다르게 입력된 것이었음.
- **"세금계산서 양식수정" 기능 완료 처리**

- **버그 수정**: `file://`로 직접 열면 브라우저가 로컬 파일 `fetch()`를 차단해 "변환 실패: Failed to fetch" 발생.
  템플릿 xlsx를 base64로 인코딩해 `tools/templates/template-data.js`에 내장하고 `atob`로 디코딩하는 방식으로 변경,
  `fetch` 의존 완전 제거. `tools/tax-invoice-convert.js`, `index.html`(스크립트 로드 순서) 수정.
  Node로 전체 파이프라인(convert() 함수 자체) 재검증 — 203건, 정답 파일과 100% 일치(H열 하이픈 규칙 포함), 실제 xlsx 출력도 정상.

- **헤더 서식 작업**: 템플릿 1~3줄에 색/테두리/폰트/정렬 적용 요청 처리 중 SheetJS 무료판이 셀 스타일 저장을
  지원하지 않는다는 사실 발견 → **ExcelJS로 전환**(`vendor/exceljs.min.js`, 기존 `xlsx.full.min.js`는 삭제).
  - 공급받는자 정보(H~V열) 연파랑(FFDDEBF7), 품목 정보(W~BB열) 연초록(FFE2EFDA), 나머지(A~G, BC~BJ)는 무색
  - A1:BJ3 전체 모든 테두리(얇은 실선) 적용
  - 전체 텍스트(헤더+데이터 모두) 맑은 고딕 10pt 적용
  - 병합된 셀(17개 범위) 전부 가로/세로 중앙 정렬 + 자동 줄바꿈
  - 스타일은 매 변환마다 코드로 다시 그리지 않고 `tools/templates/대량등록_양식_정발행_template.xlsx`에
    한 번 구워넣은 뒤 `tools/templates/template-data.js`(base64)로 재인코딩해 embed
  - Node로 전체 파이프라인 재검증 — 203건, 정답 파일(수기수정)과 100% 일치(H열 하이픈 규칙 제외),
    스타일(배경색/테두리/폰트) 정상 적용 확인

- **서식 수정**: 3번줄(개별 필드 헤더)은 배경색 없이, 4번줄(데이터 시작)부터는 테두리 없이로 변경.
  이 과정에서 이전 버전의 버그 발견 — 3번줄에 배경색을 안 넣으려 했는데 실제로는 초록색이 잘못 들어가 있었음
  (원인: 병합 셀 처리 관련 ExcelJS 동작 이슈로 추정). 원본(etc/) 파일을 기준으로 템플릿을 처음부터 다시 구워서 해결.
  이제 2번줄(그룹 라벨)만 배경색, 3번줄은 무색, 1~3번줄만 테두리, 4번줄부터는 테두리 없음. 203건 재검증 통과.

- **두 번째 기능 "복지제도 관리" 구현 완료**
  - 대상 파일: `D:\★ 하이라인닷넷\5. 인사관련\2026 복지제도 지원현황-하이라인닷넷.xls` (연차내역/복지포인트 내역/개인별지원사항 3개 시트)
  - 세금계산서 도구와 달리 "파일 변환"이 아니라 "상시 관리 페이지" 성격이라 데이터는 브라우저 localStorage에 저장,
    필요할 때 같은 컬럼 구성의 xlsx로 다운로드하는 방식으로 설계 (사용자와 범위 확인 후 진행: 3개 시트 전부 관리,
    localStorage 저장, xlsx 다운로드)
  - 구현 파일: `tools/welfare-management.js`, `vendor/xlsx.full.min.js`(재도입 — 원본이 구버전 .xls라 업로드 파싱에
    SheetJS가 필요함. 다운로드/쓰기는 기존처럼 ExcelJS 사용), `index.html`(메뉴 "복지제도 관리" + 섹션 + wf- CSS)
  - 3개 시트 컬럼 구조를 분석해서 그대로 필드로 매핑하되, 원본의 4줄짜리 병합 헤더는 관리 편의를 위해
    한 줄로 펼침 (컬럼 수/순서는 동일하게 유지 — "일반연차 사용1~14", "사용1_금액/내역/지급일 ~ 사용11" 등)
  - 연차내역: 사용/잔여/연간합계는 원본 수식과 동일하게 계산해서 읽기전용으로 자동 표시
    (사용 = 하계+보건+일반연차사용합, 잔여 = 기준-사용, 연간합계 = 사용+장기근속+예비군+경조+결혼+출산+기타).
    실제 샘플 파일(2026년, 16명)로 Node에서 전수 검증 — 0건 불일치
  - 복지포인트 내역: 잔여한도 = 지급포인트-사용합계 자동 계산, 사용내역(금액/내역/지급일)은 행별 상세 패널에서 추가/삭제.
    실제 파일 대조 시 임원 9명은 원본이 "-"로 수기 표기했지만 실제로는 사용내역이 없어 계산값(지급포인트 그대로)과
    다름 — 원본 자체의 수기 입력 관례 차이일 뿐 계산 오류 아님(사용내역 있는 행은 전부 일치 확인)
  - 개인별지원사항: 전부 단순 텍스트 필드라 계산 로직 없음
  - 팀명은 원본에서 병합 셀이라 반복되는 행에서는 빈 값으로 저장돼있는데, 파싱 시 직전 팀명으로 forward-fill 처리해
    편집 화면에서 매 행마다 팀명이 보이도록 함 (다운로드본은 병합 대신 매 행 반복 표기 — 시각적 병합은 구현 안 함,
    이 부분만 원본과 다름)
  - 업로드는 기존 파일(.xls) 또는 이 도구가 만든 다운로드본(.xlsx) 둘 다 인식 가능("성명" 헤더가 있는 행을 찾아
    그 다음줄부터 데이터로 처리 — 헤더가 몇 줄이든 대응)
  - 검증: Node로 실제 원본 파일 파싱 → 수식 재계산 → xlsx로 내보내기 → 다시 파싱까지 왕복 테스트로 데이터 무결성 확인.
    추가로 jsdom으로 실제 index.html 마크업 위에서 초기화/탭 전환/행 추가·삭제/상세패널 토글/다운로드 버튼까지
    시뮬레이션해서 런타임 에러 없음을 확인하는 과정에서 실제 버그 1건 발견 및 수정:
    `ws.columns.forEach(...)`가 일부 ExcelJS 환경(브라우저 번들 vs npm 패키지)에서 null일 수 있어
    `ws.getColumn(i+1).width = ...` 방식으로 교체 (`setColumnWidths` 헬퍼 추가)
  - 원본 파일(개인정보 포함이라 등록번호/건강검진 이력 등 민감정보 있음)은 `etc/`에 복사하지 않음 — D: 드라이브
    원본 위치 그대로 사용자가 필요할 때 업로드하는 방식 유지

- **세 번째 기능 "지로청구서 자동기안" 작업 중** — biz.giro.or.kr에 로그인해서 지정일자(10/20/25/말일)에
  해당하는 지로번호·전자납부번호를 `etc/지로납부번호.xlsx`에서 읽어 일괄 기안 등록하는 기능.
  - 이 기능은 다른 사이트 로그인/세션 조작이 필요해 브라우저 안에서 실행 불가 → `tools/giro-auto-draft/`에
    독립 Playwright(Node.js) 스크립트로 구현, 사용자가 로컬에서 직접 실행하는 방식으로 결정
    (`index.html`엔 실행 안내 페이지만 추가)
  - 엑셀 파싱 로직(`tools/giro-auto-draft/parse-excel.js`) 완성 및 검증:
    - 거래처 빈칸은 바로 위 행 값으로 fill-down
    - 지로번호 있으면 '지로번호', 없고 거래처가 "전기요금"이면 전자납부번호 값을 '고객번호'로,
      그 외(KT 등)는 '전자납부번호'로 분류
    - 납부일자 "10일자/20일자/25일자/말일자" ↔ 사용자 입력 "10/20/25/말일" 매칭 정규화
      (첫 구현 때 "말일" 매칭 버그 있었음 — 테스트로 발견, 수정 완료)
  - 로그인 자동화(`run.js`)는 로그인 폼까지 찾아 ID/PW 입력은 되지만, **실제 로그인 시도 1회가
    "비밀번호 오류"로 실패함** (계정 잠김 5회 중 1회 소진 — 사용자가 비밀번호는 맞다고 확인).
    원인 분석 결과: 이 사이트는 nppfs(plugin-free 보안모듈)로 비밀번호를 암호화해서 전송하는데,
    이 모듈이 `wss://127.0.0.1:14440`(로컬 보안 프로그램)과 통신을 시도하다가 브라우저 보안 정책
    (Private Network Access)에 막힘 → Playwright 자동화 브라우저 환경 특유의 문제로 추정,
    평소 로그인하던 실제 회사 PC에서는 해당 프로그램이 설치돼있어 정상 동작할 가능성 높음
  - **계정 잠김 위험 때문에 이 환경(샌드박스)에서는 더 이상 로그인 재시도 안 함** — 사용자가 본인 PC에
    `tools/giro-auto-draft/` 폴더를 복사해서 직접 `node run.js 20` 등으로 실행하기로 함
  - 로그인 성공 시 "기안·납부자료 작성" 화면으로 이동해서 화면 구조를 `recon-01.png`/`recon-01.json`으로
    저장하는 단계까지 구현됨 — 실제 등록 화면을 아직 본 적이 없어서 자동 입력·저장 로직(2단계)은 미구현.
    사용자가 스크립트 실행 후 recon 결과를 공유해주면 이어서 완성 예정

## 진행중
- **지로청구서 자동기안**: 사용자 PC(2026-07-16)에서 `node run.js 20` 1차 실행함.
  - **버그 발견 및 수정**: 로그인 성공 판정이 "클릭 후 예외 없음"만 보고 있었는데, 로그인 페이지와
    로그인 성공 후 페이지가 같은 URL(`index.biz`)을 써서 실제로는 로그인 실패("비밀번호 1회 오류")
    였는데도 스크립트가 "로그인 완료"로 잘못 출력함(`recon-01.png`으로 실제 로그인 실패 화면임을 확인).
    `run.js`에 `hasLoginForm()`/`findLoginErrorMessage()` 추가해서, 클릭 후에도 로그인 폼(#id)이
    남아있으면 실제 오류 메시지를 잡아 예외로 던지도록 수정 — 이제 실패 시 `main()`의 catch가 정상
    동작해서 `recon-login-fail.png` 저장 후 즉시 종료(후속 메뉴 클릭 등 진행 안 함)
  - 사용자가 사이트에 수동 로그인해서 비밀번호는 정확하고 계정도 안 잠겼음을 확인함(오류 1/5회 소진).
    즉 이번 실패의 근본 원인은 README에 적힌 known issue(nppfs 보안모듈이 로컬 보안 프로그램과 통신
    못 해 비밀번호가 서버에 제대로 전달 안 됨)일 가능성이 높음 — 코드 수정만 하고 재실행은 보류함
    (같은 이유로 또 실패하면 계정 잠김 위험이 더 커지므로)
  - 로그인 실패로 인해 실제 "기안·납부자료 작성" 등록 화면의 recon은 아직 확보 못함(`recon-01.png`/json은
    로그인 실패 페이지 캡처였음, 등록 화면 구조 아님)

- **2차 시도(2026-07-16, 보안 프로그램 실행 확인 후)**: 이번엔 비밀번호 오류가 아니라 아예 로그인 폼을
  찾지 못하고 타임아웃(`findLoginFrame`, 20초). `recon-login-fail.png` 확인 결과 "고객님의 개인정보
  보호를 위하여 암호화 작업중입니다" 화면에서 멈춰 있었음 — nppfs 보안모듈이 로컬 보안 프로그램
  (`wss://127.0.0.1:14440`)과의 확인/설치를 못 끝내고 계속 로딩 중인 상태. 보안 프로그램이 실행 중임을
  확인했는데도 이 상태라, **Playwright 자동화 브라우저 자체의 정책(Private Network Access 등)이
  localhost 통신을 막고 있을 가능성이 높음** — 단순 "프로그램 켜짐/꺼짐" 문제가 아니라 자동화 브라우저의
  구조적 한계로 추정됨. 이번엔 아이디/비밀번호 입력 전에 실패해서 서버에 로그인 시도가 전달되지
  않았음 — 오류 횟수는 그대로 1/5.

- **3차 시도 — 우회 방법 두 가지 다 실패 (2026-07-16)**:
  1. Playwright launch 플래그(`--disable-features=BlockInsecurePrivateNetworkRequests,...`) 추가 →
     동일하게 "암호화 작업중" 화면에서 멈춤. PNA 정책 문제가 아니라 `navigator.webdriver` 등으로
     자동화 브라우저 자체를 감지해서 막는 것으로 추정. (로그인 시도 전 실패, 오류 횟수 영향 없음)
  2. `launchPersistentContext()`로 사용자의 실제 Chrome 기본 프로필을 그대로 사용 시도 →
     Chrome 자체 정책에 막힘: `DevTools remote debugging requires a non-default data directory.`
     기본(Default) 데이터 디렉터리에는 원격 디버깅 연결 자체를 거부함. 우회하려면 프로필을 별도
     경로로 복사해야 하는데, 이는 지로 사이트뿐 아니라 **평소 로그인한 모든 사이트의 쿠키/저장된
     비밀번호**까지 프로젝트 폴더로 복사하는 셈이라 Claude Code 자체 안전장치가 이 복사 작업을
     차단함(사용자의 막연한 동의만으로는 부족하다고 판단).
  - 사용자와 상의 후 **로그인 자동화를 포기하고 반자동 방식으로 전환하기로 결정**.

- **반자동 방식으로 전환 완료**: `run.js`에서 Playwright/로그인 관련 코드 전부 제거.
  이제 엑셀에서 대상 목록만 정리해서 출력하고, 로그인·메뉴 이동·등록은 사용자가 직접
  평소 쓰는 브라우저로 진행하도록 안내 문구만 출력함. `README.md`에 오늘 조사한 3가지 차단
  원인(로그인 판정 버그, nppfs 자동화 감지, Chrome 기본 프로필 CDP 차단)을 전부 기록해둠.
  스테일한 `recon-01.png`/`recon-01.json`/`recon-login-fail.png`(전부 로그인 실패 화면이었음)는 삭제함.
  `node run.js 20`으로 정상 동작 확인함(대상 21건 목록 출력 + 안내 문구).

- **등록 화면(Step01~04) recon 및 입력 자동화 구현 완료 (2026-07-16)**: 사용자가 실제 "일반지로요금"
  화면을 직접 걸어가며 5장 캡처해서 공유해줌. 4단계 구조 확인:
  Step01(지로번호 입력) → Step02(전자납부번호 입력) → Step03(간략조회 결과, 상세보기) →
  Step04(상세조회 결과 및 기안, "기안하기"/"계속 기안하기"/"취소"). 기안 후 "기안문서 관리" 화면에
  쌓이며, 그 화면엔 별도로 "상신" 버튼이 있고 **당일 상신 안 하면 기안 자료가 사라짐**(사용자 확인:
  상신은 자동화 범위에서 제외, 기안까지만).
  - **버그 발견**: 지로번호가 있는 행도 실제로는 건별 전자납부번호(엑셀 E열)가 따로 있는데
    `parse-excel.js`가 이를 버리고 있었음 — 지로번호(기관 고유번호, Step01용)와 전자납부번호
    (건별 고유번호, Step02용)를 실제 등록 화면 캡처(코웨이 22,700원 건, 전자납부번호
    500000619425)와 대조해서 발견. `parse-excel.js`에서 `epay` 필드로 함께 반환하도록 수정,
    실제 엑셀로 재검증함(웅진코웨이/SK브로드밴드/SK텔레콤 21건 모두 지로번호+전자납부번호 정상 확인).
  - KT·전기요금(지로번호 없음)은 등록 방식이 다르다고 확인(사용자), 이번 자동화 범위에서 제외 —
    같은 날 함께 기안해야 해서 추후 별도로 화면 캡처받아 추가 예정.
  - **자동화 연결 방식**: 로그인은 여전히 사용자가 직접(3차 시도에서 겪은 자동화 감지/Chrome 기본
    프로필 CDP 차단 문제를 피하기 위함). `launch-chrome.bat`(신규)로 이 용도 전용의 새 Chrome
    프로필(`chrome-profile/`, 평소 프로필과 완전 분리)을 원격 디버깅 포트(9222)와 함께 띄우고,
    사용자가 그 창에서 직접 로그인하면 `run.js`가 `chromium.connectOverCDP()`로 그 브라우저에
    연결해서 로그인 완료를 자동 감지(`text=로그아웃` 표시 여부로 판단)한 뒤 자동으로 Step01~04를
    순회하며 "기안하기"까지 진행. **주의**: Step01~04의 버튼/입력 selector는 스크린샷을 보고
    추정한 것이라(실제 DOM을 열어본 적 없음) 실제 실행 시 조정이 필요할 수 있음 — 실패하면
    `debug-fail-row*.png`가 저장되도록 만들어둠. 특히 "계속 기안하기"가 실제로 직전 항목을 저장하는지는
    버튼 라벨/안내 문구로 추정만 한 상태라 첫 실행 후 기안문서관리에서 건수 검증 필요.
  - `README.md`에 오늘 조사한 로그인 자동화 차단 전체 히스토리(1~3차 시도)와 새 실행 방법을 정리함.

- **`index.html` 허브에 "오늘 기안 대상 미리보기" 추가**: 브라우저는 외부 사이트 로그인을 다룰 수
  없어 실제 자동화는 여전히 `run.js`가 담당하지만, 허브 페이지에서 엑셀을 업로드하면 오늘 날짜
  (10일/20일/25일/말일 자동 판별)에 해당하는 대상을 미리 보여주고 "지로 사이트 열기" 버튼 +
  `node run.js <날짜>` 명령 안내를 제공하도록 `tools/giro-draft-preview.js` 신규 작성,
  `index.html`의 "지로청구서 자동기안" 섹션과 스크립트 로드/`sectionInit`에 등록.
  Playwright로 실제 `file://` 로드 + 메뉴 클릭 + 파일 업로드까지 시뮬레이션해서 콘솔 에러 없음과
  파싱 결과(21건 정상 표시)까지 확인함.
  - **개선**: 매번 업로드해야 하는 게 불편하다는 피드백 반영 — `localStorage`에 파싱된 전체 행을
    저장해두고, 메뉴를 다시 열 때마다 재업로드 없이 자동으로 오늘 대상을 보여주도록 변경. 업로드
    UI는 `<details>`로 접어서 "엑셀에 수정사항이 있으면 여기서 다시 업로드"로 숨김(수정 필요할
    때만 열어서 갱신). 재방문 시 캐시에서 자동 표시되는 것까지 Playwright로 재검증함.

## 다음 할 일
- **도우미 서버는 이제 `tools/helper-server/` 하나뿐**(위 2026-07-28 통합 항목 참고). 지로/메일/HIMS
  세 메뉴 전부 이 서버(포트 8787, 경로로 구분) 하나에 의존함 — 문제 생기면 여기부터 확인.
- PC를 재시작한 뒤 통합 도우미 서버가 시작프로그램(`hiline-helper-server.vbs`)으로 정말 자동
  실행되는지 한 번 확인 필요(지금은 세션 중 수동으로 띄워둔 상태 — 꺼져있으면
  `tools/helper-server/start-server-now.bat` 필요).
- 지로청구서 자동기안(지로번호/전기요금/KT), 메일 수신함, HIMS 동기화 셋 다 이제 실사용 검증
  완료(HIMS는 2026-07-28에 실제 고객 데이터로 스크래핑/매칭/API 3단계 전부 확인, 위 항목 참고).
- **HIMS 로그인 세션이 금방 풀리는 것으로 보임**(위 HIMS 검증 항목 참고) — 실사용하다가 "동기화"
  버튼 눌렀을 때 방금 로그인했는데도 또 로그인 페이지가 뜨는 일이 반복되면, 세션 유지 시간이
  얼마나 짧은지/브라우저 종료가 원인인지 조사 필요.
- 다음 자동화 기능 필요 시 동일한 패턴(메뉴 추가 + `tools/*.js` + 필요시 `tools/templates/` — 템플릿은 `*-data.js`로 base64 내장, ExcelJS 사용)으로 확장.
  단, 외부 사이트 로그인이 필요한 기능은 독립 Playwright 스크립트로 만들 것. 버튼 클릭만으로
  실행하고 싶다면 새로 디렉터리를 만들지 말고 `tools/helper-server/server.js`에 `/새기능/*`
  경로를 추가하는 식으로 통합 서버에 얹는 게 기본 패턴(로컬 도우미 서버 난립 방지).

## 참고
- `etc/2026.07.15 세금계산서 발행건.xlsx`, `etc/대량등록 양식 (정발행).xlsx`, `etc/지로납부번호.xlsx` — 원본 업무 파일 예시 (etc/는 건드리지 않음, 템플릿은 tools/templates/에 별도 복사본 사용)
- `tools/helper-server/giro-config.json`에 지로 사이트 로그인 정보(평문) 있음 — 공유/업로드 금지
- `tools/helper-server/mail-config.json`에 메일플러그 앱 비밀번호(평문) 있음 — 공유/업로드 금지
