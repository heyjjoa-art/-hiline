# 하이라인닷넷 업무 자동화 도구

## 프로젝트 개요

회사 업무용 엑셀 파일(세금계산서, 대량등록 양식 등)을 다루는 반복 작업을 자동화하는 도구 모음.
메뉴형 홈페이지(`index.html`)를 허브로 두고, 기능을 하나씩 메뉴로 추가하며 확장한다.

## 기술 스택

- 화면(허브): 순수 HTML + CSS + JS 단일 파일 (`index.html`)
- 실제 엑셀 처리: 브라우저 안에서 JS로 직접 처리 (`tools/*.js` + SheetJS 라이브러리).
  이 PC에 Python이 설치돼 있지 않고, 드래그&드롭 업로드 → 즉시 다운로드 흐름이 서버 없이
  동작해야 하므로 클라이언트 사이드 처리로 결정했다. 파일은 서버로 전송되지 않는다.
- 엑셀 라이브러리는 **ExcelJS**(`vendor/exceljs.min.js`) 사용. SheetJS 무료판은 셀 스타일
  (배경색/테두리/폰트) 저장을 지원하지 않아(Pro 전용 기능) ExcelJS로 전환함 — 서식이 중요한
  작업이면 반드시 ExcelJS를 쓸 것
- `index.html`을 `file://`로 직접 열면 브라우저가 로컬 파일에 대한 `fetch()`를 차단하므로,
  엑셀 서식 템플릿은 `fetch`로 불러오지 말고 base64로 인코딩해 `tools/templates/*-data.js`에
  `window.XXX_B64` 형태로 내장해두고 그걸 디코딩해서 쓴다 (`atob` + `Uint8Array`)
- 템플릿 파일 자체의 헤더 서식(배경색/테두리/폰트/정렬)은 매 변환마다 코드로 다시 그리지 않고,
  `tools/templates/*.xlsx` 원본에 미리 구워넣은 뒤 base64로 재인코딩해 embed한다
- PDF 처리는 **pdf-lib**(`vendor/pdf-lib.min.js`), 여러 파일을 한 번에 내려줄 땐 **JSZip**
  (`vendor/jszip.min.js`)을 쓴다. 브라우저가 연속 다운로드를 막는 경우가 있어 파일이 여러 개
  나오는 기능은 ZIP 하나로 묶어서 내려준다.
  - PDF 썸네일 미리보기는 pdf.js가 필요한데 `file://`로 열면 워커 로딩이 막혀 쓰기 어렵다 —
    페이지 선택은 번호 체크박스/범위 입력 방식으로 한다
- **외부 사이트 로그인/조작이 필요한 기능**(예: 지로 자동기안)은 브라우저 페이지 안에서 할 수 없다
  (다른 사이트에 로그인해 세션을 다루는 건 브라우저 보안 정책상 불가능). 이런 기능은
  `tools/<기능명>/`에 독립된 `package.json`을 두고 Playwright로 구현해서, 사용자가 로컬에서
  `npm install` 후 직접 실행하는 방식으로 만든다. `index.html`에는 실행 안내 페이지만 둔다.
  - 로그인 자동화 시 계정 잠김 위험(예: 비밀번호 5회 오류 시 잠김)이 있는 사이트는 반복 시도하지
    말고, 실패 시 즉시 멈추고 사용자에게 보고할 것
  - 로그인 정보는 코드에 하드코딩하지 말고 `giro-config.json` 같은 별도 설정 파일로 분리하고,
    공유 금지 경고를 README/화면에 명시한다

## 파일 구조

```
hiline/
├── CLAUDE.md       # 이 파일
├── handoff.md      # 작업 진행 상황 기록 (항상 최신 상태 유지)
├── index.html      # 메인 허브 (좌측 메뉴 + 콘텐츠 영역)
├── vendor/         # 로컬로 내려받은 외부 라이브러리 (SheetJS 등)
├── tools/          # 기능별 변환 로직 (JS) + tools/templates/ 양식 템플릿 원본
└── etc/            # 원본 업무 엑셀 샘플 파일 (세금계산서, 대량등록 양식 등)
```

## 작업 규칙

- 모든 UI 텍스트는 **한국어**로 작성
- `index.html`은 메뉴 항목을 추가하는 것만으로 새 기능 섹션을 추가할 수 있는 구조를 유지한다
  (메뉴 설정 배열 + 섹션 렌더 함수 + 섹션별 init 함수 방식)
- 새 기능을 추가할 때: `tools/`에 변환 로직 JS 작성 → `index.html`에 메뉴 항목·섹션 HTML·init 등록 추가
- 엑셀 서식(양식) 파일이 필요한 기능은 `tools/templates/`에 원본을 복사해두고 그 파일을 기준으로 채워 넣는다
- **작업 로그 규칙**: 작업을 진행하거나 완료할 때마다 `handoff.md`를 갱신해서 최신 상태로 유지한다.
  새 세션(셸 재시작) 시작 시 가장 먼저 `handoff.md`를 확인하고, 현재 작업 현황을 사용자에게 요약해서 알려준다.

## 향후 확장 계획

- [x] 지로청구서 자동기안 완성 (실제 기안 성공까지 검증 완료. 도우미 서버는 메일/HIMS와 통합되어
  `tools/helper-server/README.md`에 있음 — `tools/giro-auto-draft/`는 2026-07-28 통합 후 삭제됨)
- [ ] 기타 반복 엑셀 작업 (요청에 따라 추가)
